[package]
name = "pbrs-grpc"
version = "0.1.0"
edition = "2021"
rust-version = "1.85"
publish = false
license = "MIT OR Apache-2.0"
description = "HTTP/2 gRPC kernel over pbrs (not tonic; protobuf-tonic stays the tonic adapter)"

[build-dependencies]
pbrs = { path = ".." }

[dependencies]
base64 = "0.22"
bytes = "1"
# Already in the graph via h2. Direct so `Streaming` can be a `Stream` without
# a per-message allocation. No C.
futures-core = { version = "0.3", default-features = false }
# `rust_backend` is miniz_oxide: pure Rust, no C in the compression path.
flate2 = { version = "1", default-features = false, features = ["rust_backend"] }
h2 = "0.4"
http = "1"
pbrs = { path = ".." }
# rustls default features pull in aws-lc-rs, which compiles C. We disable
# them and supply Graviola, which builds with rustc only.
rustls = { version = "0.23", default-features = false, features = ["std", "tls12"] }
rustls-graviola = "0.4"
rustls-pemfile = "2"
# Pure-Rust wrapper around libc socket options. Tokio already depends on it;
# a direct dep lets the kernel set SO_KEEPALIVE without compiling C.
socket2 = "0.6"
tokio = { version = "1", features = ["rt-multi-thread", "macros", "net", "time", "sync", "io-util"] }
tokio-rustls = { version = "0.26", default-features = false, features = ["tls12"] }
webpki-roots = "1"

[dev-dependencies]
# The hostile-peer tests speak raw HTTP/2 so they can send bytes no
# well-behaved client would.
bytes = "1"
flate2 = { version = "1", default-features = false, features = ["rust_backend"] }
h2 = "0.4"
http = "1"
tokio = { version = "1", features = ["rt-multi-thread", "macros", "net", "time", "sync", "io-util"] }

[[bin]]
name = "pbrs-grpc-hello"
path = "src/bin/pbrs-grpc-hello.rs"

[[bin]]
name = "pbrs-grpc-interop-server"
path = "src/bin/pbrs-grpc-interop-server.rs"

[[bin]]
name = "pbrs-grpc-interop-client"
path = "src/bin/pbrs-grpc-interop-client.rs"

[lints]
workspace = true
