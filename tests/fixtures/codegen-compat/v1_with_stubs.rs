// @generated by protoc-gen-pbrs
#[allow(unused, reason = "generated protobuf code may not exercise all fields, methods, or imports")]
#[allow(non_snake_case, reason = "protobuf field and method names follow schema conventions")]
#[allow(non_camel_case_types, reason = "protobuf message and enum names follow schema conventions")]
#[allow(non_upper_case_globals, reason = "protobuf enum value names follow schema conventions")]
#[allow(unreachable_pub, reason = "generated items are re-exported at module level")]
#[allow(clippy::all, reason = "generated protobuf code does not adhere to hand-written clippy style")]
#[allow(clippy::pedantic, reason = "generated protobuf code does not adhere to clippy pedantic rules")]
#[allow(clippy::nursery, reason = "generated protobuf code does not adhere to clippy nursery rules")]
#[allow(clippy::indexing_slicing, reason = "bounds-checked wire decoding and reflection tables")]
#[allow(clippy::cast_possible_truncation, reason = "protobuf wire format varint and field number conversions")]
#[allow(clippy::cast_possible_wrap, reason = "protobuf wire format varint and field number conversions")]
#[allow(clippy::cast_sign_loss, reason = "protobuf wire format varint and field number conversions")]
#[allow(clippy::let_underscore_must_use, reason = "ignoring results in generated wire helpers")]
mod __gen_compat_proto {
#![allow(unused, reason = "generated protobuf code may not exercise all fields, methods, or imports")]
#![allow(non_snake_case, reason = "protobuf field and method names follow schema conventions")]
#![allow(non_camel_case_types, reason = "protobuf message and enum names follow schema conventions")]
#![allow(non_upper_case_globals, reason = "protobuf enum value names follow schema conventions")]
#![allow(unreachable_pub, reason = "generated items are re-exported at module level")]
#![allow(unsafe_code, reason = "generated deserialization and transmutes use unsafe for performance")]
#![allow(clippy::all, reason = "generated protobuf code does not adhere to hand-written clippy style")]
#![allow(clippy::pedantic, reason = "generated protobuf code does not adhere to clippy pedantic rules")]
#![allow(clippy::nursery, reason = "generated protobuf code does not adhere to clippy nursery rules")]
#![allow(clippy::indexing_slicing, reason = "bounds-checked wire decoding and reflection tables")]
#![allow(clippy::cast_possible_truncation, reason = "protobuf wire format varint and field number conversions")]
#![allow(clippy::cast_possible_wrap, reason = "protobuf wire format varint and field number conversions")]
#![allow(clippy::cast_sign_loss, reason = "protobuf wire format varint and field number conversions")]
#![allow(clippy::let_underscore_must_use, reason = "ignoring results in generated wire helpers")]
use pbrs::prelude::*;
use pbrs::{Enum, Map, MapMut, MapView, ParseError, ProtoBytes, ProtoString, Repeated, RepeatedMut, RepeatedView, SerializeError, UnknownEnumValue};
use pbrs::UnknownFields;

/// FileDescriptorSet bytes for reflection and dynamic schema inspection.
pub const FILE_DESCRIPTOR_SET: &[u8] = &[
    ,
];

fn generated_pool() -> std::sync::Arc<pbrs::DescriptorPool> {
    static P: std::sync::OnceLock<std::sync::Arc<pbrs::DescriptorPool>> = std::sync::OnceLock::new();
    P.get_or_init(|| {
        std::sync::Arc::new(pbrs::DescriptorPool::from_file_descriptor_set(FILE_DESCRIPTOR_SET).expect("fds"))
    }).clone()
}

/// The `compat.Status` enum.
#[repr(transparent)]
#[derive(Clone, Copy, PartialEq, Eq, Hash, PartialOrd, Ord)]
pub struct Status(pub i32);
    #[allow(non_upper_case_globals, reason = "protobuf enum names")]
impl Status {
    ///
    /// Enum value `Unspecified` (0).
    pub const Unspecified: Status = Status(0);
    ///
    /// Enum value `Active` (1).
    pub const Active: Status = Status(1);
    ///
    /// Enum value `Pending` (2).
    pub const Pending: Status = Status(2);
    fn constant_name(self) -> Option<&'static str> {
        #[allow(unreachable_patterns, reason = "open enums")]
        Some(match self.0 {
            0 => "Unspecified",
            1 => "Active",
            2 => "Pending",
            _ => return None,
        })
    }
}
impl From<Status> for i32 {
    fn from(v: Status) -> i32 { v.0 }
}
impl From<i32> for Status {
    fn from(val: i32) -> Self { Self(val) }
}
impl Default for Status {
    fn default() -> Self { Self(0) }
}
impl std::fmt::Debug for Status {
    fn fmt(&self, f: &mut std::fmt::Formatter<'_>) -> std::fmt::Result {
        if let Some(n) = self.constant_name() { write!(f, "Status::{n}") } else { write!(f, "Status::from({})", self.0) }
    }
}
impl pbrs::__internal::SealedInternal for Status {}
impl pbrs::Proxied for Status { type View<'msg> = Status; }
impl pbrs::AsView for Status {
    type Proxied = Self;
    fn as_view(&self) -> Self { *self }
}
impl<'msg> pbrs::IntoView<'msg> for Status {
    fn into_view<'shorter>(self) -> Self where 'msg: 'shorter { self }
}
impl Enum for Status {
    const NAME: &'static str = "Status";
    fn is_known(value: i32) -> bool { matches!(value, 0 | 1 | 2) }
}
/// The `compat.CompatMessage` protobuf message.
#[derive(Clone, Debug)]
pub struct CompatMessage {
    /// Scalar fields
    id: i32,
    name: pbrs::rt::LazyStr,
    /// Optional fields with explicit presence
    description: Option<Box<pbrs::rt::LazyStr>>,
    priority: Option<i32>,
    /// Repeated fields
    tags: Repeated<pbrs::rt::LazyStr>,
    scores: pbrs::rt::PackedI32,
    /// Map field
    properties: Map<pbrs::rt::LazyStr, pbrs::rt::LazyStr>,
    /// Enum field
    status: i32,
    text_payload: Option<Box<pbrs::rt::LazyStr>>,
    number_payload: Option<i32>,
    /// Legacy field present in v1, retired in v2
    legacy_field: i32,
    unknown: UnknownFields,
    cached_size: pbrs::rt::CachedSize,
}
impl PartialEq for CompatMessage {
    fn eq(&self, other: &Self) -> bool {
        if self.id != other.id { return false; }
        if self.name != other.name { return false; }
        if self.description != other.description { return false; }
        if self.priority != other.priority { return false; }
        if self.tags != other.tags { return false; }
        if self.scores != other.scores { return false; }
        if self.properties != other.properties { return false; }
        if self.status != other.status { return false; }
        if self.text_payload != other.text_payload { return false; }
        if self.number_payload != other.number_payload { return false; }
        if self.legacy_field != other.legacy_field { return false; }
        self.unknown == other.unknown
    }
}
impl Eq for CompatMessage {}
impl Default for CompatMessage {
    #[inline(always)]
    fn default() -> Self {
        unsafe { pbrs::rt::zeroed_message() }
    }
}
impl CompatMessage {
    /// Creates a new, default instance of [`CompatMessage`].
    pub fn new() -> Self { Self::default() }
    /// Whether an empty byte slice is a valid encoding of this message.
    pub const EMPTY_PARSE_OK: bool = true;
    /// The fully-qualified protobuf name of this message.
    pub const FULL_NAME: &'static str = "compat.CompatMessage";
    /// Scalar fields
    ///
    /// Field `id` (number 1).
    ///
    /// Implicit presence field (default: `0`).
    pub fn id(&self) -> i32 { self.id }
    /// Sets the value of `id`.
    pub fn set_id(&mut self, v: i32) { self.cached_size.dirty(); self.id = v; }
    /// Field `name` (number 2).
    ///
    /// Implicit presence string (default: "").
    pub fn name(&self) -> &pbrs::ProtoStr { Some(self.name.as_view()).unwrap_or_else(|| pbrs::ProtoStr::from_bytes(b"")) }
    /// Sets the value of `name`.
    pub fn set_name(&mut self, v: impl pbrs::IntoProxied<ProtoString>) { self.cached_size.dirty(); self.name = pbrs::rt::LazyStr::owned(v.into_proxied(pbrs::__internal::Private)); }
    /// Returns `true` if field `description` is set.
    pub fn has_description(&self) -> bool { self.description.as_ref().is_some() }
    /// Optional fields with explicit presence
    ///
    /// Field `description` (number 3).
    ///
    /// Explicit optional string. Returns the string value, or the default ("") if unset.
    pub fn description(&self) -> &pbrs::ProtoStr { self.description.as_ref().map(|s| s.as_view()).unwrap_or_else(|| pbrs::ProtoStr::from_bytes(b"")) }
    /// Returns `Some` if field `description` is set, or `None` if unset.
    pub fn description_opt(&self) -> Option<&pbrs::ProtoStr> { self.description.as_ref().map(|s| s.as_view()) }
    /// Sets the value of `description`.
    pub fn set_description(&mut self, v: impl pbrs::IntoProxied<ProtoString>) {
        self.cached_size.dirty();
        self.description = Some(Box::new(pbrs::rt::LazyStr::owned(v.into_proxied(pbrs::__internal::Private))));
    }
    /// Clears field `description`, marking it unset.
    pub fn clear_description(&mut self) { self.cached_size.dirty(); self.description = None; }
    /// Returns `true` if field `priority` is set.
    pub fn has_priority(&self) -> bool { self.priority.is_some() }
    /// Field `priority` (number 4).
    ///
    /// Explicit optional field. Returns the value of `priority` or the default (`0`) if unset.
    pub fn priority(&self) -> i32 { self.priority.unwrap_or(0) }
    /// Returns `Some` if field `priority` is set, or `None` if unset.
    pub fn priority_opt(&self) -> Option<i32> { self.priority.map(|v| v) }
    /// Sets the value of `priority`.
    pub fn set_priority(&mut self, v: i32) {
        self.cached_size.dirty();
        self.priority = Some(v);
    }
    /// Clears field `priority`, marking it unset.
    pub fn clear_priority(&mut self) { self.cached_size.dirty(); self.priority = None; }
    /// Repeated fields
    ///
    /// Field `tags` (number 5).
    ///
    /// Repeated field of `pbrs::rt::LazyStr`. Empty by default.
    pub fn tags(&self) -> RepeatedView<'_, pbrs::rt::LazyStr> { self.tags.as_view() }
    /// Returns a mutable view of field `tags`.
    pub fn tags_mut(&mut self) -> RepeatedMut<'_, pbrs::rt::LazyStr> { self.cached_size.dirty(); self.tags.as_mut() }
    /// Sets the value of `tags`.
    pub fn set_tags(&mut self, v: impl IntoIterator<Item = pbrs::rt::LazyStr>) { self.cached_size.dirty(); self.tags = v.into_iter().collect(); }
    /// Field `scores` (number 6).
    ///
    /// Repeated field of `i32` (wire format: packed). Empty by default.
    pub fn scores(&self) -> RepeatedView<'_, i32> { self.scores.as_view() }
    /// Returns a mutable view of field `scores`.
    pub fn scores_mut(&mut self) -> RepeatedMut<'_, i32> { self.cached_size.dirty(); self.scores.as_mut() }
    /// Sets the value of `scores`.
    pub fn set_scores(&mut self, v: impl IntoIterator<Item = i32>) { self.cached_size.dirty(); self.scores = pbrs::rt::Packed::from_repeated(v.into_iter().collect()); }
    /// Map field
    ///
    /// Field `properties` (number 7).
    ///
    /// Map field with key `pbrs::rt::LazyStr` and value `pbrs::rt::LazyStr`. Empty by default.
    pub fn properties(&self) -> MapView<'_, pbrs::rt::LazyStr, pbrs::rt::LazyStr> { self.properties.as_view() }
    /// Returns a mutable view of field `properties`.
    pub fn properties_mut(&mut self) -> MapMut<'_, pbrs::rt::LazyStr, pbrs::rt::LazyStr> { self.cached_size.dirty(); self.properties.as_mut() }
    /// Sets the value of `properties`.
    pub fn set_properties(&mut self, v: Map<pbrs::rt::LazyStr, pbrs::rt::LazyStr>) { self.cached_size.dirty(); self.properties = v; }
    /// Enum field
    ///
    /// Field `status` (number 8).
    ///
    /// Implicit presence field (default: `0`).
    pub fn status(&self) -> Status { Status(self.status) }
    /// Sets the value of `status`.
    pub fn set_status(&mut self, v: impl Into<i32>) { self.cached_size.dirty(); self.status = v.into(); }
    /// Returns `true` if field `text_payload` is set.
    pub fn has_text_payload(&self) -> bool { self.text_payload.as_ref().is_some() }
    /// Field `text_payload` (number 9).
    ///
    /// Explicit optional string. Returns the string value, or the default ("") if unset.
    /// Part of a oneof: setting this field clears other fields in the oneof.
    pub fn text_payload(&self) -> &pbrs::ProtoStr { self.text_payload.as_ref().map(|s| s.as_view()).unwrap_or_else(|| pbrs::ProtoStr::from_bytes(b"")) }
    /// Returns `Some` if field `text_payload` is set, or `None` if unset.
    pub fn text_payload_opt(&self) -> Option<&pbrs::ProtoStr> { self.text_payload.as_ref().map(|s| s.as_view()) }
    /// Sets the value of `text_payload`.
    /// Part of a oneof: clears any other set field in the oneof.
    pub fn set_text_payload(&mut self, v: impl pbrs::IntoProxied<ProtoString>) {
        self.cached_size.dirty();
        self.number_payload = None;
        self.text_payload = Some(Box::new(pbrs::rt::LazyStr::owned(v.into_proxied(pbrs::__internal::Private))));
    }
    /// Clears field `text_payload`, marking it unset.
    pub fn clear_text_payload(&mut self) { self.cached_size.dirty(); self.text_payload = None; }
    /// Returns `true` if field `number_payload` is set.
    pub fn has_number_payload(&self) -> bool { self.number_payload.is_some() }
    /// Field `number_payload` (number 10).
    ///
    /// Explicit optional field. Returns the value of `number_payload` or the default (`0`) if unset.
    /// Part of a oneof: setting this field clears other fields in the oneof.
    pub fn number_payload(&self) -> i32 { self.number_payload.unwrap_or(0) }
    /// Returns `Some` if field `number_payload` is set, or `None` if unset.
    pub fn number_payload_opt(&self) -> Option<i32> { self.number_payload.map(|v| v) }
    /// Sets the value of `number_payload`.
    /// Part of a oneof: clears any other set field in the oneof.
    pub fn set_number_payload(&mut self, v: i32) {
        self.cached_size.dirty();
        self.text_payload = None;
        self.number_payload = Some(v);
    }
    /// Clears field `number_payload`, marking it unset.
    pub fn clear_number_payload(&mut self) { self.cached_size.dirty(); self.number_payload = None; }
    /// Legacy field present in v1, retired in v2
    ///
    /// Field `legacy_field` (number 11).
    ///
    /// Implicit presence field (default: `0`).
    pub fn legacy_field(&self) -> i32 { self.legacy_field }
    /// Sets the value of `legacy_field`.
    pub fn set_legacy_field(&mut self, v: i32) { self.cached_size.dirty(); self.legacy_field = v; }
    #[inline(always)] pub fn check_required(&self) -> Result<(), ParseError> {
        Ok(())
    }
    pub fn merge_bytes(&mut self, data: &[u8], depth: u32) -> Result<(), ParseError> { if data.is_empty() { return self.check_required(); } let mut pos = 0; let mut wire = None; self.merge_inner(data, &mut wire, &mut pos, depth, true, None) }
    pub fn merge_bytes_dont_enforce(&mut self, data: &[u8], depth: u32) -> Result<(), ParseError> { if data.is_empty() { return Ok(()); } let mut pos = 0; let mut wire = None; self.merge_inner(data, &mut wire, &mut pos, depth, false, None) }
    pub fn merge_group(&mut self, data: &[u8], wire: &mut Option<pbrs::rt::Wire>, pos: &mut usize, num: u32, depth: u32) -> Result<(), ParseError> { self.merge_inner(data, wire, pos, depth, false, Some(num)) }
    pub fn merge_inner(&mut self, data: &[u8], wire: &mut Option<pbrs::rt::Wire>, pos: &mut usize, depth: u32, enforce: bool, until: Option<u32>) -> Result<(), ParseError> {
        if depth > pbrs::RECURSION_LIMIT { return Err(ParseError::new("recursion limit exceeded")); } let _ = enforce;
        self.cached_size.dirty();
        while *pos < data.len() {
            let (n, w) = pbrs::rt::decode_tag(data, pos)?;
            if let Some(g) = until { if w == pbrs::rt::WIRE_EGROUP { if n != g { return Err(ParseError::new("mismatched end-group")); } return Ok(()); } }
            match n {
            1 => match w {
                pbrs::rt::WIRE_VARINT => {
                    self.id = pbrs::rt::decode_varint(data, pos)? as i32;
                    }
                _ => self.unknown.fields.push(pbrs::rt::capture_unknown(data, pos, n, w)?),
            }
            2 => match w {
                pbrs::rt::WIRE_LEN => {
                    let (s, e) = pbrs::rt::read_len_span(data, pos)?; self.name = pbrs::rt::LazyStr::from_parse_span(wire, data, s, e)?;
                    }
                _ => self.unknown.fields.push(pbrs::rt::capture_unknown(data, pos, n, w)?),
            }
            3 => match w {
                pbrs::rt::WIRE_LEN => {
                    let (s, e) = pbrs::rt::read_len_span(data, pos)?; self.description = Some(Box::new(pbrs::rt::LazyStr::from_parse_span(wire, data, s, e)?));
                    }
                _ => self.unknown.fields.push(pbrs::rt::capture_unknown(data, pos, n, w)?),
            }
            4 => match w {
                pbrs::rt::WIRE_VARINT => {
                    self.priority = Some(pbrs::rt::decode_varint(data, pos)? as i32);
                    }
                _ => self.unknown.fields.push(pbrs::rt::capture_unknown(data, pos, n, w)?),
            }
            5 => match w {
                pbrs::rt::WIRE_LEN => { let (s, e) = pbrs::rt::read_len_span(data, pos)?; let rest = data.len().saturating_sub(e); if rest > 2 { self.tags.reserve(1 + rest / (e - s + 2).max(1)); } self.tags.push(pbrs::rt::LazyStr::from_parse_span(wire, data, s, e)?); while *pos < data.len() { let save = *pos; match pbrs::rt::decode_tag(data, pos) { Ok((n2, w2)) if n2 == 5 && w2 == pbrs::rt::WIRE_LEN => { let (s, e) = pbrs::rt::read_len_span(data, pos)?; self.tags.push(pbrs::rt::LazyStr::from_parse_span(wire, data, s, e)?); } Ok(_) => { *pos = save; break; } Err(e) => return Err(e), } } }
                _ => self.unknown.fields.push(pbrs::rt::capture_unknown(data, pos, n, w)?),
            }
            6 => match w {
                pbrs::rt::WIRE_LEN => { let (s, e) = pbrs::rt::read_len_span(data, pos)?; self.scores.append_wire(pbrs::rt::Wire::ensure(wire, data).window(s, e))?; }
                pbrs::rt::WIRE_VARINT => { self.scores.push(pbrs::rt::decode_varint(data, pos)? as i32); let rest = data.len().saturating_sub(*pos); if rest > 2 { self.scores.reserve(rest / 3); } while *pos < data.len() { let save = *pos; match pbrs::rt::decode_tag(data, pos) { Ok((n2, w2)) if n2 == 6 && w2 == pbrs::rt::WIRE_VARINT => self.scores.push(pbrs::rt::decode_varint(data, pos)? as i32), Ok(_) => { *pos = save; break; } Err(e) => return Err(e), } } }
                _ => self.unknown.fields.push(pbrs::rt::capture_unknown(data, pos, n, w)?),
            }
            7 => match w {
                pbrs::rt::WIRE_LEN => {
                    let (s, e) = pbrs::rt::read_len_span(data, pos)?;
                    let (kk, vv) = decode_map_entry_CompatMessage_properties_7(&pbrs::rt::Wire::ensure(wire, data).window(s, e), depth + 1)?;
                    self.properties.push_entry(kk, vv);
                }
                _ => self.unknown.fields.push(pbrs::rt::capture_unknown(data, pos, n, w)?),
            }
            8 => match w {
                pbrs::rt::WIRE_VARINT => {
                    self.status = pbrs::rt::decode_varint(data, pos)? as i32;
                    }
                _ => self.unknown.fields.push(pbrs::rt::capture_unknown(data, pos, n, w)?),
            }
            9 => match w {
                pbrs::rt::WIRE_LEN => {
        self.number_payload = None;
                    let (s, e) = pbrs::rt::read_len_span(data, pos)?; self.text_payload = Some(Box::new(pbrs::rt::LazyStr::from_parse_span(wire, data, s, e)?));
                    }
                _ => self.unknown.fields.push(pbrs::rt::capture_unknown(data, pos, n, w)?),
            }
            10 => match w {
                pbrs::rt::WIRE_VARINT => {
        self.text_payload = None;
                    self.number_payload = Some(pbrs::rt::decode_varint(data, pos)? as i32);
                    }
                _ => self.unknown.fields.push(pbrs::rt::capture_unknown(data, pos, n, w)?),
            }
            11 => match w {
                pbrs::rt::WIRE_VARINT => {
                    self.legacy_field = pbrs::rt::decode_varint(data, pos)? as i32;
                    }
                _ => self.unknown.fields.push(pbrs::rt::capture_unknown(data, pos, n, w)?),
            }
                _ => self.unknown.fields.push(pbrs::rt::capture_unknown(data, pos, n, w)?),
            }
        }
        if until.is_some() { return Err(ParseError::new("truncated group")); }
        if enforce { self.check_required()?; }
        Ok(())
    }
    pub fn validate_inner(wire: &pbrs::rt::Wire, pos: &mut usize, depth: u32) -> Result<(), ParseError> { Self::validate_until(wire, pos, depth, None) }
    pub fn validate_until(wire: &pbrs::rt::Wire, pos: &mut usize, depth: u32, until: Option<u32>) -> Result<(), ParseError> {
        if depth > pbrs::RECURSION_LIMIT { return Err(ParseError::new("recursion limit exceeded")); }
        let data = wire.as_slice();
        while *pos < data.len() {
            let (n, w) = pbrs::rt::decode_tag(data, pos)?;
            if let Some(g) = until { if w == pbrs::rt::WIRE_EGROUP { if n != g { return Err(ParseError::new("mismatched end-group")); } return Ok(()); } }
            match n {
            1 => match w {
                pbrs::rt::WIRE_VARINT => { let _ = pbrs::rt::decode_varint(data, pos)? as i32; }
                _ => pbrs::rt::skip_field(data, pos, w)?,
            }
            2 => match w {
                pbrs::rt::WIRE_LEN => { let (s, e) = pbrs::rt::read_len_span(data, pos)?; pbrs::rt::require_utf8(&data[s..e])?; }
                _ => pbrs::rt::skip_field(data, pos, w)?,
            }
            3 => match w {
                pbrs::rt::WIRE_LEN => { let (s, e) = pbrs::rt::read_len_span(data, pos)?; pbrs::rt::require_utf8(&data[s..e])?; }
                _ => pbrs::rt::skip_field(data, pos, w)?,
            }
            4 => match w {
                pbrs::rt::WIRE_VARINT => { let _ = pbrs::rt::decode_varint(data, pos)? as i32; }
                _ => pbrs::rt::skip_field(data, pos, w)?,
            }
            5 => match w {
                pbrs::rt::WIRE_LEN => { let (s, e) = pbrs::rt::read_len_span(data, pos)?; pbrs::rt::require_utf8(&data[s..e])?; }
                _ => pbrs::rt::skip_field(data, pos, w)?,
            }
            6 => match w {
                pbrs::rt::WIRE_LEN => { let (s, e) = pbrs::rt::read_len_span(data, pos)?; pbrs::rt::PackedI32::validate_bytes(&data[s..e])?; }
                pbrs::rt::WIRE_VARINT => { let _ = pbrs::rt::decode_varint(data, pos)? as i32; }
                _ => pbrs::rt::skip_field(data, pos, w)?,
            }
            7 => match w {
                pbrs::rt::WIRE_LEN => { let (s, e) = pbrs::rt::read_len_span(data, pos)?; let mut ip = 0; let w = wire.window(s, e); let d = w.as_slice(); while ip < d.len() { let (_, ww) = pbrs::rt::decode_tag(d, &mut ip)?; pbrs::rt::skip_field(d, &mut ip, ww)?; } }
                _ => pbrs::rt::skip_field(data, pos, w)?,
            }
            8 => match w {
                pbrs::rt::WIRE_VARINT => { let _ = pbrs::rt::decode_varint(data, pos)? as i32; }
                _ => pbrs::rt::skip_field(data, pos, w)?,
            }
            9 => match w {
                pbrs::rt::WIRE_LEN => { let (s, e) = pbrs::rt::read_len_span(data, pos)?; pbrs::rt::require_utf8(&data[s..e])?; }
                _ => pbrs::rt::skip_field(data, pos, w)?,
            }
            10 => match w {
                pbrs::rt::WIRE_VARINT => { let _ = pbrs::rt::decode_varint(data, pos)? as i32; }
                _ => pbrs::rt::skip_field(data, pos, w)?,
            }
            11 => match w {
                pbrs::rt::WIRE_VARINT => { let _ = pbrs::rt::decode_varint(data, pos)? as i32; }
                _ => pbrs::rt::skip_field(data, pos, w)?,
            }
                _ => pbrs::rt::skip_field(data, pos, w)?,
            }
        }
        if until.is_some() { return Err(ParseError::new("truncated group")); }
        Ok(())
    }
    pub fn compute_size(&self) -> u64 {
        if let Some(n) = self.cached_size.get() { return n; }
        let mut n = self.unknown.encoded_len();
        if self.id != 0 { n += pbrs::rt::tag_len(1, pbrs::rt::WIRE_VARINT) + pbrs::rt::varint_len((self.id) as u64); }
        if !self.name.is_empty() { n += pbrs::rt::key_len_value_len(2, self.name.as_bytes().len() as u64); }
        if let Some(s) = &self.description { n += pbrs::rt::key_len_value_len(3, s.as_bytes().len() as u64); }
        if let Some(v) = self.priority { n += pbrs::rt::tag_len(4, pbrs::rt::WIRE_VARINT) + pbrs::rt::varint_len((v) as u64); }
        for t in self.tags.iter() { n += pbrs::rt::key_len_value_len(5, t.as_bytes().len() as u64); }
        if let Some(p) = self.scores.packed_bytes() { n += pbrs::rt::key_len_value_len(6, p.len() as u64); } else if !self.scores.is_empty() { let mut payload = 0u64; for t in self.scores.iter() { payload += pbrs::rt::varint_len((*t) as u64); } n += pbrs::rt::key_len_value_len(6, payload); }
        if !self.properties.is_empty() {
        for (k, v) in self.properties.pairs() {
            let inner = pbrs::rt::key_len_value_len(1, k.as_bytes().len() as u64) + pbrs::rt::key_len_value_len(2, v.as_bytes().len() as u64);
            n += pbrs::rt::key_len_value_len(7, inner);
        }
        }
        if self.status != 0 { n += pbrs::rt::tag_len(8, pbrs::rt::WIRE_VARINT) + pbrs::rt::varint_len((self.status) as u64); }
        if let Some(s) = &self.text_payload { n += pbrs::rt::key_len_value_len(9, s.as_bytes().len() as u64); }
        if let Some(v) = self.number_payload { n += pbrs::rt::tag_len(10, pbrs::rt::WIRE_VARINT) + pbrs::rt::varint_len((v) as u64); }
        if self.legacy_field != 0 { n += pbrs::rt::tag_len(11, pbrs::rt::WIRE_VARINT) + pbrs::rt::varint_len((self.legacy_field) as u64); }
        self.cached_size.set(n);
        n
    }
    pub fn write_to(&self, out: &mut impl pbrs::rt::WireOut) {
        if self.id != 0 { pbrs::rt::encode_tag(out, 1, pbrs::rt::WIRE_VARINT); pbrs::rt::encode_varint(out, self.id as u64); }
        if !self.name.is_empty() { pbrs::rt::encode_len_field(out, 2, self.name.as_bytes()); }
        if let Some(s) = &self.description { pbrs::rt::encode_len_field(out, 3, s.as_bytes()); }
        if let Some(v) = self.priority { pbrs::rt::encode_tag(out, 4, pbrs::rt::WIRE_VARINT); pbrs::rt::encode_varint(out, v as u64); }
        for t in self.tags.iter() { pbrs::rt::encode_len_field(out, 5, t.as_bytes()); }
        if let Some(p) = self.scores.packed_bytes() { pbrs::rt::encode_len_header(out, 6, p.len() as u64); out.extend_from_slice(p); } else if !self.scores.is_empty() { let mut payload = 0u64; for t in self.scores.iter() { payload += pbrs::rt::varint_len((*t) as u64); } pbrs::rt::encode_len_header(out, 6, payload); for t in self.scores.iter() { pbrs::rt::encode_varint(out, *t as u64); } }
        if !self.properties.is_empty() {
        for (k, v) in self.properties.pairs() {
            let inner = pbrs::rt::key_len_value_len(1, k.as_bytes().len() as u64) + pbrs::rt::key_len_value_len(2, v.as_bytes().len() as u64);
            pbrs::rt::encode_tag(out, 7, pbrs::rt::WIRE_LEN); pbrs::rt::encode_varint(out, inner);
            pbrs::rt::encode_len_field(out, 1, k.as_bytes());
            pbrs::rt::encode_len_field(out, 2, v.as_bytes());
        }
        }
        if self.status != 0 { pbrs::rt::encode_tag(out, 8, pbrs::rt::WIRE_VARINT); pbrs::rt::encode_varint(out, self.status as u64); }
        if let Some(s) = &self.text_payload { pbrs::rt::encode_len_field(out, 9, s.as_bytes()); }
        if let Some(v) = self.number_payload { pbrs::rt::encode_tag(out, 10, pbrs::rt::WIRE_VARINT); pbrs::rt::encode_varint(out, v as u64); }
        if self.legacy_field != 0 { pbrs::rt::encode_tag(out, 11, pbrs::rt::WIRE_VARINT); pbrs::rt::encode_varint(out, self.legacy_field as u64); }
        self.unknown.encode(out);
    }
    pub fn to_json(&self) -> Result<String, SerializeError> {
        Ok(self.to_json_value()?.to_string())
    }
    pub fn from_json(json: &str) -> Result<Self, ParseError> { Self::from_json_ignore(json, false) }
    pub fn from_json_ignore(json: &str, ignore: bool) -> Result<Self, ParseError> {
        let v = pbrs::json::parse(json)?;
        Self::from_json_value(&v, ignore)
    }
    pub fn to_json_value(&self) -> Result<pbrs::json::Json, SerializeError> {
        let mut map = pbrs::json::JsonMap::new();
        if self.id() != 0 {
            let _ = map.insert("id".into(), pbrs::json::int32(self.id()));
        }
        if !self.name().as_bytes().is_empty() {
            let _ = map.insert("name".into(), pbrs::json::string(self.name().as_bytes()));
        }
        if self.has_description() {
            let _ = map.insert("description".into(), pbrs::json::string(self.description().as_bytes()));
        }
        if self.has_priority() {
            let _ = map.insert("priority".into(), pbrs::json::int32(self.priority()));
        }
        if !self.tags().is_empty() {
            let mut arr = Vec::new();
            for item in self.tags() {
                arr.push(pbrs::json::string(item.as_bytes()));
            }
            let _ = map.insert("tags".into(), pbrs::json::Json::Array(arr));
        }
        if !self.scores().is_empty() {
            let mut arr = Vec::new();
            for item in self.scores() {
                arr.push(pbrs::json::int32(item));
            }
            let _ = map.insert("scores".into(), pbrs::json::Json::Array(arr));
        }
        if !self.properties().is_empty() {
            let mut obj = pbrs::json::JsonMap::new();
            for (k, v) in self.properties() {
                let _ = obj.insert(String::from_utf8_lossy(k.as_bytes()).into_owned(), pbrs::json::string(v.as_bytes()));
            }
            let _ = map.insert("properties".into(), pbrs::json::Json::Object(obj));
        }
        if i32::from(self.status()) != 0 {
            let _ = map.insert("status".into(), { let n = i32::from(self.status()); pbrs::json::enumeration(match n { 0 => Some("STATUS_UNSPECIFIED"), 1 => Some("STATUS_ACTIVE"), 2 => Some("STATUS_PENDING"), _ => None }, n) });
        }
        if self.has_text_payload() {
            let _ = map.insert("textPayload".into(), pbrs::json::string(self.text_payload().as_bytes()));
        }
        if self.has_number_payload() {
            let _ = map.insert("numberPayload".into(), pbrs::json::int32(self.number_payload()));
        }
        if self.legacy_field() != 0 {
            let _ = map.insert("legacyField".into(), pbrs::json::int32(self.legacy_field()));
        }
        Ok(pbrs::json::Json::Object(map))
    }
    pub fn from_json_value(v: &pbrs::json::Json, ignore: bool) -> Result<Self, ParseError> {
        let obj = v.as_object().ok_or_else(|| ParseError::new("json message must be an object"))?;
        let mut msg = Self::new();
        let mut seen = std::collections::BTreeSet::<u32>::new();
        for (key, val) in obj {
            match key.as_str() {
                "id" => {
                    if !seen.insert(1u32) { return Err(ParseError::owned(format!("duplicate json field {key}"))); }
                    if val.is_null() { continue; }
                    msg.set_id(pbrs::json::as_i32(val)?);
                }
                "name" => {
                    if !seen.insert(2u32) { return Err(ParseError::owned(format!("duplicate json field {key}"))); }
                    if val.is_null() { continue; }
                    msg.set_name(pbrs::json::as_str(val)?);
                }
                "description" => {
                    if !seen.insert(3u32) { return Err(ParseError::owned(format!("duplicate json field {key}"))); }
                    if val.is_null() { continue; }
                    msg.set_description(pbrs::json::as_str(val)?);
                }
                "priority" => {
                    if !seen.insert(4u32) { return Err(ParseError::owned(format!("duplicate json field {key}"))); }
                    if val.is_null() { continue; }
                    msg.set_priority(pbrs::json::as_i32(val)?);
                }
                "tags" => {
                    if !seen.insert(5u32) { return Err(ParseError::owned(format!("duplicate json field {key}"))); }
                    if val.is_null() { continue; }
                    let arr = val.as_array().ok_or_else(|| ParseError::new("json repeated must be an array"))?;
                    for item in arr {
                        msg.tags_mut().push(pbrs::json::as_str(item)?);
                    }
                }
                "scores" => {
                    if !seen.insert(6u32) { return Err(ParseError::owned(format!("duplicate json field {key}"))); }
                    if val.is_null() { continue; }
                    let arr = val.as_array().ok_or_else(|| ParseError::new("json repeated must be an array"))?;
                    for item in arr {
                        msg.scores_mut().push(pbrs::json::as_i32(item)?);
                    }
                }
                "properties" => {
                    if !seen.insert(7u32) { return Err(ParseError::owned(format!("duplicate json field {key}"))); }
                    if val.is_null() { continue; }
                    let obj = val.as_object().ok_or_else(|| ParseError::new("json map must be an object"))?;
                    for (k, v) in obj {
                        msg.properties_mut().insert(k.as_str(), pbrs::json::as_str(v)?);
                    }
                }
                "status" => {
                    if !seen.insert(8u32) { return Err(ParseError::owned(format!("duplicate json field {key}"))); }
                    if val.is_null() { continue; }
                    if let Some(n) = pbrs::json::as_enum(val, ignore, |s| match s { "STATUS_ACTIVE" => Some(1), "STATUS_PENDING" => Some(2), "STATUS_UNSPECIFIED" => Some(0), _ => None })? {
                        msg.set_status(n);
                    }
                }
                "textPayload" | "text_payload" => {
                    if !seen.insert(9u32) { return Err(ParseError::owned(format!("duplicate json field {key}"))); }
                    if val.is_null() { continue; }
                    if msg.has_number_payload() { return Err(ParseError::new("duplicate oneof member")); }
                    msg.set_text_payload(pbrs::json::as_str(val)?);
                }
                "numberPayload" | "number_payload" => {
                    if !seen.insert(10u32) { return Err(ParseError::owned(format!("duplicate json field {key}"))); }
                    if val.is_null() { continue; }
                    if msg.has_text_payload() { return Err(ParseError::new("duplicate oneof member")); }
                    msg.set_number_payload(pbrs::json::as_i32(val)?);
                }
                "legacyField" | "legacy_field" => {
                    if !seen.insert(11u32) { return Err(ParseError::owned(format!("duplicate json field {key}"))); }
                    if val.is_null() { continue; }
                    msg.set_legacy_field(pbrs::json::as_i32(val)?);
                }
                _ => {
                    if !ignore { return Err(ParseError::owned(format!("unknown json field {key}"))); }
                }
            }
        }
        Ok(msg)
    }
    pub fn to_text(&self) -> Result<String, SerializeError> {
        let mut out = String::new();
        self.write_text(&mut out, 0)?;
        Ok(out)
    }
    pub fn to_text_with_unknown(&self) -> Result<String, SerializeError> {
        let mut out = String::new();
        self.write_text(&mut out, 0)?;
        pbrs::text::write_unknown_fields(&self.unknown, &mut out, 0);
        Ok(out)
    }
    pub fn from_text(text: &str) -> Result<Self, ParseError> {
        Self::from_text_value(&pbrs::text::parse(text)?)
    }
    pub fn write_text(&self, out: &mut String, indent: usize) -> Result<(), SerializeError> {
        if self.id() != 0 {
            pbrs::text::write_named_int32(out, indent, "id", self.id());
        }
        if !self.name().as_bytes().is_empty() {
            pbrs::text::write_named_string(out, indent, "name", self.name().as_bytes());
        }
        if self.has_description() {
            pbrs::text::write_named_string(out, indent, "description", self.description().as_bytes());
        }
        if self.has_priority() {
            pbrs::text::write_named_int32(out, indent, "priority", self.priority());
        }
        for item in self.tags() {
            pbrs::text::write_named_string(out, indent, "tags", item.as_bytes());
        }
        for item in self.scores() {
            pbrs::text::write_named_int32(out, indent, "scores", item);
        }
        if !self.properties().is_empty() {
            let mut items: Vec<_> = self.properties().iter().collect();
            items.sort_by(|(a, _), (b, _)| a.as_bytes().cmp(b.as_bytes()));
            for (k, v) in items {
                pbrs::text::write_map_entry(out, indent, "properties", |out| { pbrs::text::write_bytes_lit(k.as_bytes(), out); }, |out| { pbrs::text::write_bytes_lit(v.as_bytes(), out); Ok(()) }, false)?;
            }
        }
        if i32::from(self.status()) != 0 {
            { let n = i32::from(self.status()); pbrs::text::write_named_enum(out, indent, "status", match n { 0 => Some("STATUS_UNSPECIFIED"), 1 => Some("STATUS_ACTIVE"), 2 => Some("STATUS_PENDING"), _ => None }, n); };
        }
        if self.has_text_payload() {
            pbrs::text::write_named_string(out, indent, "text_payload", self.text_payload().as_bytes());
        }
        if self.has_number_payload() {
            pbrs::text::write_named_int32(out, indent, "number_payload", self.number_payload());
        }
        if self.legacy_field() != 0 {
            pbrs::text::write_named_int32(out, indent, "legacy_field", self.legacy_field());
        }
        Ok(())
    }
    pub fn from_text_value(fields: &[(String, pbrs::text::TextValue)]) -> Result<Self, ParseError> {
        let mut msg = Self::new();
        for (key, val) in fields {
            match key.as_str() {
                "id" => {
                    msg.set_id(val.as_i32()?);
                }
                "name" => {
                    msg.set_name(val.as_str()?);
                }
                "description" => {
                    msg.set_description(val.as_str()?);
                }
                "priority" => {
                    msg.set_priority(val.as_i32()?);
                }
                "tags" => {
                    if let Some(items) = val.as_list() {
                        for item in items {
                            msg.tags_mut().push(item.as_str()?);
                        }
                    } else {
                        msg.tags_mut().push(val.as_str()?);
                    }
                }
                "scores" => {
                    if let Some(items) = val.as_list() {
                        for item in items {
                            msg.scores_mut().push(item.as_i32()?);
                        }
                    } else {
                        msg.scores_mut().push(val.as_i32()?);
                    }
                }
                "properties" => {
                    let entry = val.as_message()?;
                    let mut k = "";
                    let mut v = "";
                    for (ek, ev) in entry {
                        match ek.as_str() {
                            "key" => k = ev.as_str()?,
                            "value" => v = ev.as_str()?,
                            _ => return Err(ParseError::owned(format!("unknown field {ek}"))),
                        }
                    }
                    msg.properties_mut().insert(k, v);
                }
                "status" => {
                    msg.set_status(val.as_enum(|s| match s { "STATUS_ACTIVE" => Some(1), "STATUS_PENDING" => Some(2), "STATUS_UNSPECIFIED" => Some(0), _ => None })?);
                }
                "text_payload" => {
                    msg.set_text_payload(val.as_str()?);
                }
                "number_payload" => {
                    msg.set_number_payload(val.as_i32()?);
                }
                "legacy_field" => {
                    msg.set_legacy_field(val.as_i32()?);
                }
                _ => return Err(ParseError::owned(format!("unknown field {key}"))),
            }
        }
        Ok(msg)
    }
}
pbrs::impl_typed_message!(CompatMessage, CompatMessageView, CompatMessageMut);
fn decode_map_entry_CompatMessage_properties_7(wire: &pbrs::rt::Wire, depth: u32) -> Result<(pbrs::rt::LazyStr, pbrs::rt::LazyStr), ParseError> {
    let _ = depth; let data = wire.as_slice(); let mut key = pbrs::rt::LazyStr::default(); let mut val = pbrs::rt::LazyStr::default(); let mut pos = 0;
    while pos < data.len() { let (n, w) = pbrs::rt::decode_tag(data, &mut pos)?; match (n, w) {
        (1, pbrs::rt::WIRE_LEN) => { let (s, e) = pbrs::rt::read_len_span(data, &mut pos)?; pbrs::rt::require_utf8(&data[s..e])?; key = pbrs::rt::LazyStr::from_span(wire, s, e); },
        (2, pbrs::rt::WIRE_LEN) => { let (s, e) = pbrs::rt::read_len_span(data, &mut pos)?; pbrs::rt::require_utf8(&data[s..e])?; val = pbrs::rt::LazyStr::from_span(wire, s, e); },
        _ => pbrs::rt::skip_field(data, &mut pos, w)?,
    } }
    Ok((key, val))
}
/// The `compat.CompatRequest` protobuf message.
#[derive(Clone, Debug)]
pub struct CompatRequest {
    query: pbrs::rt::LazyStr,
    page: i32,
    unknown: UnknownFields,
    cached_size: pbrs::rt::CachedSize,
}
impl PartialEq for CompatRequest {
    fn eq(&self, other: &Self) -> bool {
        if self.query != other.query { return false; }
        if self.page != other.page { return false; }
        self.unknown == other.unknown
    }
}
impl Eq for CompatRequest {}
impl Default for CompatRequest {
    #[inline(always)]
    fn default() -> Self {
        unsafe { pbrs::rt::zeroed_message() }
    }
}
impl CompatRequest {
    /// Creates a new, default instance of [`CompatRequest`].
    pub fn new() -> Self { Self::default() }
    /// Whether an empty byte slice is a valid encoding of this message.
    pub const EMPTY_PARSE_OK: bool = true;
    /// The fully-qualified protobuf name of this message.
    pub const FULL_NAME: &'static str = "compat.CompatRequest";
    /// Field `query` (number 1).
    ///
    /// Implicit presence string (default: "").
    pub fn query(&self) -> &pbrs::ProtoStr { Some(self.query.as_view()).unwrap_or_else(|| pbrs::ProtoStr::from_bytes(b"")) }
    /// Sets the value of `query`.
    pub fn set_query(&mut self, v: impl pbrs::IntoProxied<ProtoString>) { self.cached_size.dirty(); self.query = pbrs::rt::LazyStr::owned(v.into_proxied(pbrs::__internal::Private)); }
    /// Field `page` (number 2).
    ///
    /// Implicit presence field (default: `0`).
    pub fn page(&self) -> i32 { self.page }
    /// Sets the value of `page`.
    pub fn set_page(&mut self, v: i32) { self.cached_size.dirty(); self.page = v; }
    #[inline(always)] pub fn check_required(&self) -> Result<(), ParseError> {
        Ok(())
    }
    pub fn merge_bytes(&mut self, data: &[u8], depth: u32) -> Result<(), ParseError> { if data.is_empty() { return self.check_required(); } let mut pos = 0; let mut wire = None; self.merge_inner(data, &mut wire, &mut pos, depth, true, None) }
    pub fn merge_bytes_dont_enforce(&mut self, data: &[u8], depth: u32) -> Result<(), ParseError> { if data.is_empty() { return Ok(()); } let mut pos = 0; let mut wire = None; self.merge_inner(data, &mut wire, &mut pos, depth, false, None) }
    pub fn merge_group(&mut self, data: &[u8], wire: &mut Option<pbrs::rt::Wire>, pos: &mut usize, num: u32, depth: u32) -> Result<(), ParseError> { self.merge_inner(data, wire, pos, depth, false, Some(num)) }
    pub fn merge_inner(&mut self, data: &[u8], wire: &mut Option<pbrs::rt::Wire>, pos: &mut usize, depth: u32, enforce: bool, until: Option<u32>) -> Result<(), ParseError> {
        if depth > pbrs::RECURSION_LIMIT { return Err(ParseError::new("recursion limit exceeded")); } let _ = enforce;
        self.cached_size.dirty();
        while *pos < data.len() {
            let (n, w) = pbrs::rt::decode_tag(data, pos)?;
            if let Some(g) = until { if w == pbrs::rt::WIRE_EGROUP { if n != g { return Err(ParseError::new("mismatched end-group")); } return Ok(()); } }
            if n == 2 {
                match w {
                pbrs::rt::WIRE_VARINT => {
                    self.page = pbrs::rt::decode_varint(data, pos)? as i32;
                    }
                    _ => self.unknown.fields.push(pbrs::rt::capture_unknown(data, pos, n, w)?),
                }
                continue;
            }
            self.merge_heavy(n, w, data, wire, pos, depth)?;
        }
        if until.is_some() { return Err(ParseError::new("truncated group")); }
        if enforce { self.check_required()?; }
        Ok(())
    }
    #[inline(never)]
    fn merge_heavy(&mut self, n: u32, w: u32, data: &[u8], wire: &mut Option<pbrs::rt::Wire>, pos: &mut usize, depth: u32) -> Result<(), ParseError> {
        match n {
            1 => match w {
                pbrs::rt::WIRE_LEN => {
                    let (s, e) = pbrs::rt::read_len_span(data, pos)?; self.query = pbrs::rt::LazyStr::from_parse_span(wire, data, s, e)?;
                    }
                _ => self.unknown.fields.push(pbrs::rt::capture_unknown(data, pos, n, w)?),
            }
            _ => self.unknown.fields.push(pbrs::rt::capture_unknown(data, pos, n, w)?),
        }
        Ok(())
    }
    pub fn validate_inner(wire: &pbrs::rt::Wire, pos: &mut usize, depth: u32) -> Result<(), ParseError> { Self::validate_until(wire, pos, depth, None) }
    pub fn validate_until(wire: &pbrs::rt::Wire, pos: &mut usize, depth: u32, until: Option<u32>) -> Result<(), ParseError> {
        if depth > pbrs::RECURSION_LIMIT { return Err(ParseError::new("recursion limit exceeded")); }
        let data = wire.as_slice();
        while *pos < data.len() {
            let (n, w) = pbrs::rt::decode_tag(data, pos)?;
            if let Some(g) = until { if w == pbrs::rt::WIRE_EGROUP { if n != g { return Err(ParseError::new("mismatched end-group")); } return Ok(()); } }
            match n {
            1 => match w {
                pbrs::rt::WIRE_LEN => { let (s, e) = pbrs::rt::read_len_span(data, pos)?; pbrs::rt::require_utf8(&data[s..e])?; }
                _ => pbrs::rt::skip_field(data, pos, w)?,
            }
            2 => match w {
                pbrs::rt::WIRE_VARINT => { let _ = pbrs::rt::decode_varint(data, pos)? as i32; }
                _ => pbrs::rt::skip_field(data, pos, w)?,
            }
                _ => pbrs::rt::skip_field(data, pos, w)?,
            }
        }
        if until.is_some() { return Err(ParseError::new("truncated group")); }
        Ok(())
    }
    pub fn compute_size(&self) -> u64 {
        if let Some(n) = self.cached_size.get() { return n; }
        let mut n = self.unknown.encoded_len();
        if !self.query.is_empty() { n += pbrs::rt::key_len_value_len(1, self.query.as_bytes().len() as u64); }
        if self.page != 0 { n += pbrs::rt::tag_len(2, pbrs::rt::WIRE_VARINT) + pbrs::rt::varint_len((self.page) as u64); }
        self.cached_size.set(n);
        n
    }
    pub fn write_to(&self, out: &mut impl pbrs::rt::WireOut) {
        if !self.query.is_empty() { pbrs::rt::encode_len_field(out, 1, self.query.as_bytes()); }
        if self.page != 0 { pbrs::rt::encode_tag(out, 2, pbrs::rt::WIRE_VARINT); pbrs::rt::encode_varint(out, self.page as u64); }
        self.unknown.encode(out);
    }
    pub fn to_json(&self) -> Result<String, SerializeError> {
        Ok(self.to_json_value()?.to_string())
    }
    pub fn from_json(json: &str) -> Result<Self, ParseError> { Self::from_json_ignore(json, false) }
    pub fn from_json_ignore(json: &str, ignore: bool) -> Result<Self, ParseError> {
        let v = pbrs::json::parse(json)?;
        Self::from_json_value(&v, ignore)
    }
    pub fn to_json_value(&self) -> Result<pbrs::json::Json, SerializeError> {
        let mut map = pbrs::json::JsonMap::new();
        if !self.query().as_bytes().is_empty() {
            let _ = map.insert("query".into(), pbrs::json::string(self.query().as_bytes()));
        }
        if self.page() != 0 {
            let _ = map.insert("page".into(), pbrs::json::int32(self.page()));
        }
        Ok(pbrs::json::Json::Object(map))
    }
    pub fn from_json_value(v: &pbrs::json::Json, ignore: bool) -> Result<Self, ParseError> {
        let obj = v.as_object().ok_or_else(|| ParseError::new("json message must be an object"))?;
        let mut msg = Self::new();
        let mut seen = std::collections::BTreeSet::<u32>::new();
        for (key, val) in obj {
            match key.as_str() {
                "query" => {
                    if !seen.insert(1u32) { return Err(ParseError::owned(format!("duplicate json field {key}"))); }
                    if val.is_null() { continue; }
                    msg.set_query(pbrs::json::as_str(val)?);
                }
                "page" => {
                    if !seen.insert(2u32) { return Err(ParseError::owned(format!("duplicate json field {key}"))); }
                    if val.is_null() { continue; }
                    msg.set_page(pbrs::json::as_i32(val)?);
                }
                _ => {
                    if !ignore { return Err(ParseError::owned(format!("unknown json field {key}"))); }
                }
            }
        }
        Ok(msg)
    }
    pub fn to_text(&self) -> Result<String, SerializeError> {
        let mut out = String::new();
        self.write_text(&mut out, 0)?;
        Ok(out)
    }
    pub fn to_text_with_unknown(&self) -> Result<String, SerializeError> {
        let mut out = String::new();
        self.write_text(&mut out, 0)?;
        pbrs::text::write_unknown_fields(&self.unknown, &mut out, 0);
        Ok(out)
    }
    pub fn from_text(text: &str) -> Result<Self, ParseError> {
        Self::from_text_value(&pbrs::text::parse(text)?)
    }
    pub fn write_text(&self, out: &mut String, indent: usize) -> Result<(), SerializeError> {
        if !self.query().as_bytes().is_empty() {
            pbrs::text::write_named_string(out, indent, "query", self.query().as_bytes());
        }
        if self.page() != 0 {
            pbrs::text::write_named_int32(out, indent, "page", self.page());
        }
        Ok(())
    }
    pub fn from_text_value(fields: &[(String, pbrs::text::TextValue)]) -> Result<Self, ParseError> {
        let mut msg = Self::new();
        for (key, val) in fields {
            match key.as_str() {
                "query" => {
                    msg.set_query(val.as_str()?);
                }
                "page" => {
                    msg.set_page(val.as_i32()?);
                }
                _ => return Err(ParseError::owned(format!("unknown field {key}"))),
            }
        }
        Ok(msg)
    }
}
pbrs::impl_typed_message!(CompatRequest, CompatRequestView, CompatRequestMut);
/// The `compat.CompatResponse` protobuf message.
#[derive(Clone, Debug)]
pub struct CompatResponse {
    result: pbrs::rt::LazyStr,
    code: i32,
    unknown: UnknownFields,
    cached_size: pbrs::rt::CachedSize,
}
impl PartialEq for CompatResponse {
    fn eq(&self, other: &Self) -> bool {
        if self.result != other.result { return false; }
        if self.code != other.code { return false; }
        self.unknown == other.unknown
    }
}
impl Eq for CompatResponse {}
impl Default for CompatResponse {
    #[inline(always)]
    fn default() -> Self {
        unsafe { pbrs::rt::zeroed_message() }
    }
}
impl CompatResponse {
    /// Creates a new, default instance of [`CompatResponse`].
    pub fn new() -> Self { Self::default() }
    /// Whether an empty byte slice is a valid encoding of this message.
    pub const EMPTY_PARSE_OK: bool = true;
    /// The fully-qualified protobuf name of this message.
    pub const FULL_NAME: &'static str = "compat.CompatResponse";
    /// Field `result` (number 1).
    ///
    /// Implicit presence string (default: "").
    pub fn result(&self) -> &pbrs::ProtoStr { Some(self.result.as_view()).unwrap_or_else(|| pbrs::ProtoStr::from_bytes(b"")) }
    /// Sets the value of `result`.
    pub fn set_result(&mut self, v: impl pbrs::IntoProxied<ProtoString>) { self.cached_size.dirty(); self.result = pbrs::rt::LazyStr::owned(v.into_proxied(pbrs::__internal::Private)); }
    /// Field `code` (number 2).
    ///
    /// Implicit presence field (default: `0`).
    pub fn code(&self) -> i32 { self.code }
    /// Sets the value of `code`.
    pub fn set_code(&mut self, v: i32) { self.cached_size.dirty(); self.code = v; }
    #[inline(always)] pub fn check_required(&self) -> Result<(), ParseError> {
        Ok(())
    }
    pub fn merge_bytes(&mut self, data: &[u8], depth: u32) -> Result<(), ParseError> { if data.is_empty() { return self.check_required(); } let mut pos = 0; let mut wire = None; self.merge_inner(data, &mut wire, &mut pos, depth, true, None) }
    pub fn merge_bytes_dont_enforce(&mut self, data: &[u8], depth: u32) -> Result<(), ParseError> { if data.is_empty() { return Ok(()); } let mut pos = 0; let mut wire = None; self.merge_inner(data, &mut wire, &mut pos, depth, false, None) }
    pub fn merge_group(&mut self, data: &[u8], wire: &mut Option<pbrs::rt::Wire>, pos: &mut usize, num: u32, depth: u32) -> Result<(), ParseError> { self.merge_inner(data, wire, pos, depth, false, Some(num)) }
    pub fn merge_inner(&mut self, data: &[u8], wire: &mut Option<pbrs::rt::Wire>, pos: &mut usize, depth: u32, enforce: bool, until: Option<u32>) -> Result<(), ParseError> {
        if depth > pbrs::RECURSION_LIMIT { return Err(ParseError::new("recursion limit exceeded")); } let _ = enforce;
        self.cached_size.dirty();
        while *pos < data.len() {
            let (n, w) = pbrs::rt::decode_tag(data, pos)?;
            if let Some(g) = until { if w == pbrs::rt::WIRE_EGROUP { if n != g { return Err(ParseError::new("mismatched end-group")); } return Ok(()); } }
            if n == 2 {
                match w {
                pbrs::rt::WIRE_VARINT => {
                    self.code = pbrs::rt::decode_varint(data, pos)? as i32;
                    }
                    _ => self.unknown.fields.push(pbrs::rt::capture_unknown(data, pos, n, w)?),
                }
                continue;
            }
            self.merge_heavy(n, w, data, wire, pos, depth)?;
        }
        if until.is_some() { return Err(ParseError::new("truncated group")); }
        if enforce { self.check_required()?; }
        Ok(())
    }
    #[inline(never)]
    fn merge_heavy(&mut self, n: u32, w: u32, data: &[u8], wire: &mut Option<pbrs::rt::Wire>, pos: &mut usize, depth: u32) -> Result<(), ParseError> {
        match n {
            1 => match w {
                pbrs::rt::WIRE_LEN => {
                    let (s, e) = pbrs::rt::read_len_span(data, pos)?; self.result = pbrs::rt::LazyStr::from_parse_span(wire, data, s, e)?;
                    }
                _ => self.unknown.fields.push(pbrs::rt::capture_unknown(data, pos, n, w)?),
            }
            _ => self.unknown.fields.push(pbrs::rt::capture_unknown(data, pos, n, w)?),
        }
        Ok(())
    }
    pub fn validate_inner(wire: &pbrs::rt::Wire, pos: &mut usize, depth: u32) -> Result<(), ParseError> { Self::validate_until(wire, pos, depth, None) }
    pub fn validate_until(wire: &pbrs::rt::Wire, pos: &mut usize, depth: u32, until: Option<u32>) -> Result<(), ParseError> {
        if depth > pbrs::RECURSION_LIMIT { return Err(ParseError::new("recursion limit exceeded")); }
        let data = wire.as_slice();
        while *pos < data.len() {
            let (n, w) = pbrs::rt::decode_tag(data, pos)?;
            if let Some(g) = until { if w == pbrs::rt::WIRE_EGROUP { if n != g { return Err(ParseError::new("mismatched end-group")); } return Ok(()); } }
            match n {
            1 => match w {
                pbrs::rt::WIRE_LEN => { let (s, e) = pbrs::rt::read_len_span(data, pos)?; pbrs::rt::require_utf8(&data[s..e])?; }
                _ => pbrs::rt::skip_field(data, pos, w)?,
            }
            2 => match w {
                pbrs::rt::WIRE_VARINT => { let _ = pbrs::rt::decode_varint(data, pos)? as i32; }
                _ => pbrs::rt::skip_field(data, pos, w)?,
            }
                _ => pbrs::rt::skip_field(data, pos, w)?,
            }
        }
        if until.is_some() { return Err(ParseError::new("truncated group")); }
        Ok(())
    }
    pub fn compute_size(&self) -> u64 {
        if let Some(n) = self.cached_size.get() { return n; }
        let mut n = self.unknown.encoded_len();
        if !self.result.is_empty() { n += pbrs::rt::key_len_value_len(1, self.result.as_bytes().len() as u64); }
        if self.code != 0 { n += pbrs::rt::tag_len(2, pbrs::rt::WIRE_VARINT) + pbrs::rt::varint_len((self.code) as u64); }
        self.cached_size.set(n);
        n
    }
    pub fn write_to(&self, out: &mut impl pbrs::rt::WireOut) {
        if !self.result.is_empty() { pbrs::rt::encode_len_field(out, 1, self.result.as_bytes()); }
        if self.code != 0 { pbrs::rt::encode_tag(out, 2, pbrs::rt::WIRE_VARINT); pbrs::rt::encode_varint(out, self.code as u64); }
        self.unknown.encode(out);
    }
    pub fn to_json(&self) -> Result<String, SerializeError> {
        Ok(self.to_json_value()?.to_string())
    }
    pub fn from_json(json: &str) -> Result<Self, ParseError> { Self::from_json_ignore(json, false) }
    pub fn from_json_ignore(json: &str, ignore: bool) -> Result<Self, ParseError> {
        let v = pbrs::json::parse(json)?;
        Self::from_json_value(&v, ignore)
    }
    pub fn to_json_value(&self) -> Result<pbrs::json::Json, SerializeError> {
        let mut map = pbrs::json::JsonMap::new();
        if !self.result().as_bytes().is_empty() {
            let _ = map.insert("result".into(), pbrs::json::string(self.result().as_bytes()));
        }
        if self.code() != 0 {
            let _ = map.insert("code".into(), pbrs::json::int32(self.code()));
        }
        Ok(pbrs::json::Json::Object(map))
    }
    pub fn from_json_value(v: &pbrs::json::Json, ignore: bool) -> Result<Self, ParseError> {
        let obj = v.as_object().ok_or_else(|| ParseError::new("json message must be an object"))?;
        let mut msg = Self::new();
        let mut seen = std::collections::BTreeSet::<u32>::new();
        for (key, val) in obj {
            match key.as_str() {
                "result" => {
                    if !seen.insert(1u32) { return Err(ParseError::owned(format!("duplicate json field {key}"))); }
                    if val.is_null() { continue; }
                    msg.set_result(pbrs::json::as_str(val)?);
                }
                "code" => {
                    if !seen.insert(2u32) { return Err(ParseError::owned(format!("duplicate json field {key}"))); }
                    if val.is_null() { continue; }
                    msg.set_code(pbrs::json::as_i32(val)?);
                }
                _ => {
                    if !ignore { return Err(ParseError::owned(format!("unknown json field {key}"))); }
                }
            }
        }
        Ok(msg)
    }
    pub fn to_text(&self) -> Result<String, SerializeError> {
        let mut out = String::new();
        self.write_text(&mut out, 0)?;
        Ok(out)
    }
    pub fn to_text_with_unknown(&self) -> Result<String, SerializeError> {
        let mut out = String::new();
        self.write_text(&mut out, 0)?;
        pbrs::text::write_unknown_fields(&self.unknown, &mut out, 0);
        Ok(out)
    }
    pub fn from_text(text: &str) -> Result<Self, ParseError> {
        Self::from_text_value(&pbrs::text::parse(text)?)
    }
    pub fn write_text(&self, out: &mut String, indent: usize) -> Result<(), SerializeError> {
        if !self.result().as_bytes().is_empty() {
            pbrs::text::write_named_string(out, indent, "result", self.result().as_bytes());
        }
        if self.code() != 0 {
            pbrs::text::write_named_int32(out, indent, "code", self.code());
        }
        Ok(())
    }
    pub fn from_text_value(fields: &[(String, pbrs::text::TextValue)]) -> Result<Self, ParseError> {
        let mut msg = Self::new();
        for (key, val) in fields {
            match key.as_str() {
                "result" => {
                    msg.set_result(val.as_str()?);
                }
                "code" => {
                    msg.set_code(val.as_i32()?);
                }
                _ => return Err(ParseError::owned(format!("unknown field {key}"))),
            }
        }
        Ok(msg)
    }
}
pbrs::impl_typed_message!(CompatResponse, CompatResponseView, CompatResponseMut);

// --- gRPC stubs (pbrs-grpc kernel) ---
/// The `compat.CompatService` service.
///
/// Implement this, then serve it with [`CompatServiceServer`].
///
/// Methods you omit return [`::pbrs_grpc::Status::unimplemented`].
pub trait CompatService: Send + Sync + 'static {
    /// Bidirectional-streaming call: `BidiStreamCall`.
    /// Streaming signature: Bidirectional-streaming stream of `CompatRequest` -> stream of `CompatResponse`.
    ///
    /// The default answers [`::pbrs_grpc::Status::unimplemented`].
    /// Inbound `grpc-encoding` is [`::pbrs_grpc::Request::encoding`] (`None` for identity).
    /// Spawned work should await [`::pbrs_grpc::Request::cancelled`]; the kernel drops a still-pending handler on client RST.
    /// [`::pbrs_grpc::Status::from_error_details`] is the typed bag after a generated handler Err; those trailers reach the client.
    /// Distinct from a generated server intercept Err: that is trailers without reading the body; this generated handler Err is after the handler ran.
    /// Distinct from a generated intercept Err: that is a local reject never opens a stream; this generated handler Err is after the handler ran.
    /// Distinct from a generated server on_response Err: that is trailers-only after handler Ok; this generated handler Err is after the handler ran.
    /// Distinct from a generated client on_response Err: that fails the Call after a successful receive; this generated handler Err is after the handler ran.
    /// Distinct from a generated StreamSender fail: that is trailers after any messages already sent; this generated handler Err is after the handler ran.
    /// Spawned producers should select on [`::pbrs_grpc::Request::cancelled`] and [`::pbrs_grpc::StreamSender::closed`]; drain aborts on client RST.
    /// [`::pbrs_grpc::StreamSender::fail`] after a message ships trailing metadata and `grpc-status-details-bin` the same as a handler `Err`.
    /// [`::pbrs_grpc::Status::from_error_details`] is the typed bag after a generated StreamSender fail on a server response producer; those trailers ship after any messages already sent.
    /// Distinct from a generated handler Err: that is after the handler ran; this generated StreamSender fail is trailers after any messages already sent.
    /// Distinct from a generated server intercept Err: that is trailers without reading the body; this generated StreamSender fail is trailers after any messages already sent.
    /// Distinct from a generated server on_response Err: that is trailers-only after handler Ok; this generated StreamSender fail is trailers after any messages already sent.
    /// Distinct from a generated intercept Err: that is a local reject never opens a stream; this generated StreamSender fail is trailers after any messages already sent.
    /// Distinct from a generated client on_response Err: that fails the Call after a successful receive; this generated StreamSender fail is trailers after any messages already sent.
    fn bidi_stream_call(
        &self,
        request: ::pbrs_grpc::Request<::pbrs_grpc::Streaming<CompatRequest>>,
    ) -> impl ::core::future::Future<Output = ::core::result::Result<::pbrs_grpc::Response<::pbrs_grpc::Streaming<CompatResponse>>, ::pbrs_grpc::Status>> + Send {
        async move {
            drop(request);
            ::core::result::Result::Err(::pbrs_grpc::Status::unimplemented("method compat.CompatService/BidiStreamCall not implemented"))
        }
    }
    /// Client-streaming call: `ClientStreamCall`.
    /// Streaming signature: Client-streaming stream of `CompatRequest` -> `CompatResponse`.
    ///
    /// The default answers [`::pbrs_grpc::Status::unimplemented`].
    /// Inbound `grpc-encoding` is [`::pbrs_grpc::Request::encoding`] (`None` for identity).
    /// Spawned work should await [`::pbrs_grpc::Request::cancelled`]; the kernel drops a still-pending handler on client RST.
    /// [`::pbrs_grpc::Status::from_error_details`] is the typed bag after a generated handler Err; those trailers reach the client.
    /// Distinct from a generated server intercept Err: that is trailers without reading the body; this generated handler Err is after the handler ran.
    /// Distinct from a generated intercept Err: that is a local reject never opens a stream; this generated handler Err is after the handler ran.
    /// Distinct from a generated server on_response Err: that is trailers-only after handler Ok; this generated handler Err is after the handler ran.
    /// Distinct from a generated client on_response Err: that fails the Call after a successful receive; this generated handler Err is after the handler ran.
    /// Distinct from a generated StreamSender fail: that is trailers after any messages already sent; this generated handler Err is after the handler ran.
    /// That signal still fires after the client half-closes the request stream.
    fn client_stream_call(
        &self,
        request: ::pbrs_grpc::Request<::pbrs_grpc::Streaming<CompatRequest>>,
    ) -> impl ::core::future::Future<Output = ::core::result::Result<::pbrs_grpc::Response<CompatResponse>, ::pbrs_grpc::Status>> + Send {
        async move {
            drop(request);
            ::core::result::Result::Err(::pbrs_grpc::Status::unimplemented("method compat.CompatService/ClientStreamCall not implemented"))
        }
    }
    /// Server-streaming call: `ServerStreamCall`.
    /// Streaming signature: Server-streaming `CompatRequest` -> stream of `CompatResponse`.
    ///
    /// The default answers [`::pbrs_grpc::Status::unimplemented`].
    /// Inbound `grpc-encoding` is [`::pbrs_grpc::Request::encoding`] (`None` for identity).
    /// Spawned work should await [`::pbrs_grpc::Request::cancelled`]; the kernel drops a still-pending handler on client RST.
    /// [`::pbrs_grpc::Status::from_error_details`] is the typed bag after a generated handler Err; those trailers reach the client.
    /// Distinct from a generated server intercept Err: that is trailers without reading the body; this generated handler Err is after the handler ran.
    /// Distinct from a generated intercept Err: that is a local reject never opens a stream; this generated handler Err is after the handler ran.
    /// Distinct from a generated server on_response Err: that is trailers-only after handler Ok; this generated handler Err is after the handler ran.
    /// Distinct from a generated client on_response Err: that fails the Call after a successful receive; this generated handler Err is after the handler ran.
    /// Distinct from a generated StreamSender fail: that is trailers after any messages already sent; this generated handler Err is after the handler ran.
    /// Spawned producers should select on [`::pbrs_grpc::Request::cancelled`] and [`::pbrs_grpc::StreamSender::closed`]; drain aborts on client RST.
    /// [`::pbrs_grpc::StreamSender::fail`] after a message ships trailing metadata and `grpc-status-details-bin` the same as a handler `Err`.
    /// [`::pbrs_grpc::Status::from_error_details`] is the typed bag after a generated StreamSender fail on a server response producer; those trailers ship after any messages already sent.
    /// Distinct from a generated handler Err: that is after the handler ran; this generated StreamSender fail is trailers after any messages already sent.
    /// Distinct from a generated server intercept Err: that is trailers without reading the body; this generated StreamSender fail is trailers after any messages already sent.
    /// Distinct from a generated server on_response Err: that is trailers-only after handler Ok; this generated StreamSender fail is trailers after any messages already sent.
    /// Distinct from a generated intercept Err: that is a local reject never opens a stream; this generated StreamSender fail is trailers after any messages already sent.
    /// Distinct from a generated client on_response Err: that fails the Call after a successful receive; this generated StreamSender fail is trailers after any messages already sent.
    fn server_stream_call(
        &self,
        request: ::pbrs_grpc::Request<CompatRequest>,
    ) -> impl ::core::future::Future<Output = ::core::result::Result<::pbrs_grpc::Response<::pbrs_grpc::Streaming<CompatResponse>>, ::pbrs_grpc::Status>> + Send {
        async move {
            drop(request);
            ::core::result::Result::Err(::pbrs_grpc::Status::unimplemented("method compat.CompatService/ServerStreamCall not implemented"))
        }
    }
    /// Unary call: `UnaryCall`.
    /// Streaming signature: Unary `CompatRequest` -> `CompatResponse`.
    ///
    /// The default answers [`::pbrs_grpc::Status::unimplemented`].
    /// Inbound `grpc-encoding` is [`::pbrs_grpc::Request::encoding`] (`None` for identity).
    /// Spawned work should await [`::pbrs_grpc::Request::cancelled`]; the kernel drops a still-pending handler on client RST.
    /// [`::pbrs_grpc::Status::from_error_details`] is the typed bag after a generated handler Err; those trailers reach the client.
    /// Distinct from a generated server intercept Err: that is trailers without reading the body; this generated handler Err is after the handler ran.
    /// Distinct from a generated intercept Err: that is a local reject never opens a stream; this generated handler Err is after the handler ran.
    /// Distinct from a generated server on_response Err: that is trailers-only after handler Ok; this generated handler Err is after the handler ran.
    /// Distinct from a generated client on_response Err: that fails the Call after a successful receive; this generated handler Err is after the handler ran.
    /// Distinct from a generated StreamSender fail: that is trailers after any messages already sent; this generated handler Err is after the handler ran.
    fn unary_call(
        &self,
        request: ::pbrs_grpc::Request<CompatRequest>,
    ) -> impl ::core::future::Future<Output = ::core::result::Result<::pbrs_grpc::Response<CompatResponse>, ::pbrs_grpc::Status>> + Send {
        async move {
            drop(request);
            ::core::result::Result::Err(::pbrs_grpc::Status::unimplemented("method compat.CompatService/UnaryCall not implemented"))
        }
    }
}
/// Serves an implementation of [`CompatService`].
///
/// [`Self::rpc_timeout`], [`Self::compresses_outbound`], [`Self::gzip_level`], [`Self::accepts_compressed`], [`Self::concurrent_rpc_limit`], [`Self::send_buffer_size`], and [`Self::limits`] read the server overlay without colliding with the setters. Same getters as [`::pbrs_grpc::Server`] / [`::pbrs_grpc::Router`].
pub struct CompatServiceServer<T> {
    inner: ::std::sync::Arc<T>,
    config: ::pbrs_grpc::ServerConfig,
}
impl<T> ::core::clone::Clone for CompatServiceServer<T> {
    fn clone(&self) -> Self {
        Self {
            inner: ::std::sync::Arc::clone(&self.inner),
            config: self.config,
        }
    }
}
impl<T> CompatServiceServer<T> {
    /// Wrap an existing `Arc` without adding another layer.
    #[must_use]
    pub fn from_arc(inner: ::std::sync::Arc<T>) -> Self {
        Self {
            inner,
            config: <::pbrs_grpc::ServerConfig as ::core::default::Default>::default(),
        }
    }
    /// Take the inner `Arc` back.
    #[must_use]
    pub fn into_inner(self) -> ::std::sync::Arc<T> { self.inner }
}
impl<T> ::core::fmt::Debug for CompatServiceServer<T> {
    fn fmt(&self, f: &mut ::core::fmt::Formatter<'_>) -> ::core::fmt::Result {
        f.debug_struct("CompatServiceServer").field("service", &"compat.CompatService").field("config", &self.config).finish()
    }
}
impl<T: CompatService> CompatServiceServer<T> {
    /// Fully qualified proto service name.
    pub const NAME: &'static str = "compat.CompatService";
    /// Wrap an implementation with default configuration.
    #[must_use]
    pub fn new(inner: T) -> Self {
        Self {
            inner: ::std::sync::Arc::new(inner),
            config: <::pbrs_grpc::ServerConfig as ::core::default::Default>::default(),
        }
    }
    /// Replace the transport and limit configuration.
    #[must_use]
    pub fn config(mut self, config: ::pbrs_grpc::ServerConfig) -> Self { self.config = config; self }
    /// The configuration in effect. Distinct from [`Self::config`], which replaces it. See [`::pbrs_grpc::Server::server_config`].
    #[must_use]
    pub fn server_config(&self) -> ::pbrs_grpc::ServerConfig { self.config }
    /// Cap inbound messages at `limit` bytes. Default 4 MiB. Applies to every call shape, including over TLS, mTLS, Unix, and [`::pbrs_grpc::Server::serve_connection`].
    #[must_use]
    pub fn max_decoding_message_size(mut self, limit: usize) -> Self { self.config = self.config.max_decoding_message_size(limit); self }
    /// Cap outbound messages at `limit` bytes. Default unlimited. Applies to every call shape.
    #[must_use]
    pub fn max_encoding_message_size(mut self, limit: usize) -> Self { self.config = self.config.max_encoding_message_size(limit); self }
    /// Replace both message caps at once. Applies to every call shape. See [`::pbrs_grpc::ServerConfig::message_limits`].
    /// Distinct from [`Self::max_decoding_message_size`] / [`Self::max_encoding_message_size`]. Oversize inbound or outbound is `RESOURCE_EXHAUSTED` on every call shape, including over TLS, mTLS, Unix, and [`::pbrs_grpc::Server::serve_connection`].
    #[must_use]
    pub fn message_limits(mut self, limits: ::pbrs_grpc::MessageLimits) -> Self { self.config = self.config.message_limits(limits); self }
    /// Configured message caps. Distinct from [`Self::message_limits`], which sets them. Distinct from [`Self::send_buffer_size`]: that is the HTTP/2 send buffer, not uncompressed protobuf bytes. See [`::pbrs_grpc::Server::limits`]. Applies to every call shape.
    #[must_use]
    pub fn limits(&self) -> ::pbrs_grpc::MessageLimits { self.config.limits() }
    /// Cap how many RPCs the process will run at once. Applies to every call shape, including over TLS, mTLS, Unix, and [`::pbrs_grpc::Server::serve_connection`]. See [`::pbrs_grpc::ServerConfig::max_concurrent_rpcs`].
    #[must_use]
    pub fn max_concurrent_rpcs(mut self, n: usize) -> Self { self.config = self.config.max_concurrent_rpcs(n); self }
    /// Configured process-wide RPC cap, if any. Distinct from [`Self::max_concurrent_rpcs`], which sets it. See [`::pbrs_grpc::Server::concurrent_rpc_limit`]. Applies to every call shape.
    #[must_use]
    pub fn concurrent_rpc_limit(&self) -> ::core::option::Option<usize> { self.config.concurrent_rpc_limit() }
    /// Cap how many TCP/Unix connections the accept loop will serve at once, including TLS and mTLS listeners. Applies to every call shape. See [`::pbrs_grpc::ServerConfig::max_concurrent_connections`].
    #[must_use]
    pub fn max_concurrent_connections(mut self, n: usize) -> Self { self.config = self.config.max_concurrent_connections(n); self }
    /// Concurrent RPCs allowed per HTTP/2 connection. Applies to every call shape. See [`::pbrs_grpc::ServerConfig::max_concurrent_streams`].
    /// HTTP/2 `SETTINGS_MAX_CONCURRENT_STREAMS`. Distinct from [`Self::max_concurrent_rpcs`], which refuses extras as `RESOURCE_EXHAUSTED`. A well-behaved client waits; both RPCs still complete, including over TLS, mTLS, Unix, and [`::pbrs_grpc::Server::serve_connection`].
    #[must_use]
    pub fn max_concurrent_streams(mut self, streams: u32) -> Self { self.config = self.config.max_concurrent_streams(streams); self }
    /// HTTP/2 per-stream receive window. Applies to every call shape. See [`::pbrs_grpc::ServerConfig::initial_stream_window_size`].
    /// A well-behaved client still completes every call shape, including over TLS, mTLS, Unix, and [`::pbrs_grpc::Server::serve_connection`]. Distinct from [`Self::max_frame_size`], which still serves at the 16 KiB SETTINGS minimum, and from [`Self::max_concurrent_streams`], which serializes extra RPCs.
    #[must_use]
    pub fn initial_stream_window_size(mut self, bytes: u32) -> Self { self.config = self.config.initial_stream_window_size(bytes); self }
    /// HTTP/2 per-connection receive window. Applies to every call shape. See [`::pbrs_grpc::ServerConfig::initial_connection_window_size`].
    /// A well-behaved client still completes every call shape, including over TLS, mTLS, Unix, and [`::pbrs_grpc::Server::serve_connection`]. Distinct from [`Self::max_frame_size`], which still serves at the 16 KiB SETTINGS minimum, and from [`Self::max_concurrent_streams`], which serializes extra RPCs.
    #[must_use]
    pub fn initial_connection_window_size(mut self, bytes: u32) -> Self { self.config = self.config.initial_connection_window_size(bytes); self }
    /// HTTP/2 `SETTINGS_MAX_FRAME_SIZE`. Applies to every call shape. See [`::pbrs_grpc::ServerConfig::max_frame_size`].
    /// A well-behaved client splits DATA; every call shape still completes, including over TLS, mTLS, Unix, and [`::pbrs_grpc::Server::serve_connection`]. Distinct from [`Self::max_header_list_size`], which refuses oversize metadata, and from [`Self::max_concurrent_streams`], which serializes extra RPCs.
    #[must_use]
    pub fn max_frame_size(mut self, bytes: u32) -> Self { self.config = self.config.max_frame_size(bytes); self }
    /// HTTP/2 `SETTINGS_MAX_HEADER_LIST_SIZE`. Applies to every call shape. See [`::pbrs_grpc::ServerConfig::max_header_list_size`].
    /// Oversize metadata is refused, including over TLS, mTLS, Unix, and [`::pbrs_grpc::Server::serve_connection`]. Distinct from a raw HTTP/2 peer.
    #[must_use]
    pub fn max_header_list_size(mut self, bytes: u32) -> Self { self.config = self.config.max_header_list_size(bytes); self }
    /// HTTP/2 `SETTINGS_HEADER_TABLE_SIZE` (HPACK dynamic table). Default 4096. Applies to every call shape. Distinct from [`Self::max_header_list_size`], which caps uncompressed header-block bytes (`SETTINGS_MAX_HEADER_LIST_SIZE`). A well-behaved client still completes every call shape at this table size, including over TLS, mTLS, Unix, and [`::pbrs_grpc::Server::serve_connection`]. See [`::pbrs_grpc::ServerConfig::header_table_size`].
    #[must_use]
    pub fn header_table_size(mut self, bytes: u32) -> Self { self.config = self.config.header_table_size(bytes); self }
    /// HTTP/2 small-DATA framing budget. Default 25600. Applies to every call shape. Caps extra memory from tiny DATA frames. Exceeding this is `ENHANCE_YOUR_CALM` (`too_many_data_frames`). Distinct from [`Self::initial_connection_window_size`], which is flow-control bytes, and from [`Self::max_frame_size`], which caps one DATA payload. h2 Auto (half the connection window) is not exposed. A well-behaved client still completes every call shape at this framing budget, including over TLS, mTLS, Unix, and [`::pbrs_grpc::Server::serve_connection`]. See [`::pbrs_grpc::ServerConfig::data_frame_budget`].
    #[must_use]
    pub fn data_frame_budget(mut self, bytes: usize) -> Self { self.config = self.config.data_frame_budget(bytes); self }
    /// Per-connection HTTP/2 send buffer. Applies to every call shape. See [`::pbrs_grpc::ServerConfig::max_send_buffer_size`].
    /// Write backpressure still completes every call shape, including over TLS, mTLS, Unix, and [`::pbrs_grpc::Server::serve_connection`]. Distinct from [`Self::max_frame_size`], which still serves at the 16 KiB SETTINGS minimum, and from [`Self::initial_stream_window_size`], which still serves at a small receive window.
    #[must_use]
    pub fn max_send_buffer_size(mut self, bytes: usize) -> Self { self.config = self.config.max_send_buffer_size(bytes); self }
    /// Configured write-time HTTP/2 send buffer. Distinct from [`Self::max_send_buffer_size`], which sets it. Distinct from [`Self::message_limits`]: that is uncompressed protobuf bytes, not this send buffer. See [`::pbrs_grpc::Server::send_buffer_size`]. Applies to every call shape.
    #[must_use]
    pub fn send_buffer_size(&self) -> usize { self.config.send_buffer_size() }
    /// Cap remotely-reset HTTP/2 streams waiting in the accept queue. Applies to every call shape. See [`::pbrs_grpc::ServerConfig::max_pending_accept_reset_streams`].
    /// A well-behaved client never fills that queue; every call shape still completes, including over TLS, mTLS, Unix, and [`::pbrs_grpc::Server::serve_connection`]. Distinct from a raw HTTP/2 peer.
    #[must_use]
    pub fn max_pending_accept_reset_streams(mut self, n: usize) -> Self { self.config = self.config.max_pending_accept_reset_streams(n); self }
    /// Cap locally-reset HTTP/2 streams caused by a peer protocol error. Default 1024. Exceeding this is `ENHANCE_YOUR_CALM`. Distinct from [`Self::max_pending_accept_reset_streams`], which caps remotely-reset streams (rapid reset). This caps RSTs we send after an invalid frame. A well-behaved client never triggers one; every call shape still completes, including over TLS, mTLS, Unix, and [`::pbrs_grpc::Server::serve_connection`]. See [`::pbrs_grpc::ServerConfig::max_local_error_reset_streams`].
    #[must_use]
    pub fn max_local_error_reset_streams(mut self, n: usize) -> Self { self.config = self.config.max_local_error_reset_streams(n); self }
    /// Cap remembered locally-reset HTTP/2 stream IDs. Default 50. When the cap is reached, the oldest ID is purged from memory, not `ENHANCE_YOUR_CALM`. Frames on a purged ID are a connection `PROTOCOL_ERROR`. Distinct from [`Self::max_pending_accept_reset_streams`] (rapid-reset GOAWAY) and [`Self::max_local_error_reset_streams`] (protocol-error RST GOAWAY). A well-behaved client still completes every call shape at this memory cap, including over TLS, mTLS, Unix, and [`::pbrs_grpc::Server::serve_connection`]. See [`::pbrs_grpc::ServerConfig::max_concurrent_reset_streams`].
    #[must_use]
    pub fn max_concurrent_reset_streams(mut self, n: usize) -> Self { self.config = self.config.max_concurrent_reset_streams(n); self }
    /// How long locally-reset HTTP/2 stream IDs are remembered. Default 1 s. After this duration the ID is forgotten, not `ENHANCE_YOUR_CALM`. Frames on a forgotten ID are a connection `PROTOCOL_ERROR`. Distinct from [`Self::max_concurrent_reset_streams`], which is how many IDs are remembered (count). This is how long (time). A well-behaved client still completes every call shape at this reset duration, including over TLS, mTLS, Unix, and [`::pbrs_grpc::Server::serve_connection`]. See [`::pbrs_grpc::ServerConfig::reset_stream_duration`].
    #[must_use]
    pub fn reset_stream_duration(mut self, dur: ::std::time::Duration) -> Self { self.config = self.config.reset_stream_duration(dur); self }
    /// Cap every RPC even when the client omits `grpc-timeout`. Applies to every call shape, including over TLS, mTLS, Unix, and [`::pbrs_grpc::Server::serve_connection`]. See [`::pbrs_grpc::ServerConfig::timeout`].
    #[must_use]
    pub fn timeout(mut self, timeout: ::std::time::Duration) -> Self { self.config = self.config.timeout(timeout); self }
    /// gzip responses when the client advertises gzip. Applies to every call shape, including over TLS, mTLS, Unix, and [`::pbrs_grpc::Server::serve_connection`]. See [`::pbrs_grpc::ServerConfig::send_compressed`].
    #[must_use]
    pub fn send_compressed(mut self) -> Self { self.config = self.config.send_compressed(true); self }
    /// Deflate effort for outbound gzip. Default 1 (`flate2` fast). Applies to every call shape. Distinct from [`Self::send_compressed`], which is on or off. 0 stores; 9 is best. A well-behaved client still completes every call shape, including over TLS, mTLS, Unix, and [`::pbrs_grpc::Server::serve_connection`]. See [`::pbrs_grpc::ServerConfig::gzip_compression_level`].
    #[must_use]
    pub fn gzip_compression_level(mut self, level: u32) -> Self { self.config = self.config.gzip_compression_level(level); self }
    /// Inflate inbound gzip. Default `true`. Applies to every call shape, including over TLS, mTLS, Unix, and [`::pbrs_grpc::Server::serve_connection`]. Passing `false` refuses `grpc-encoding: gzip` as `UNIMPLEMENTED` before the handler. Distinct from [`Self::send_compressed`], which is outbound. See [`::pbrs_grpc::ServerConfig::accept_compressed`].
    #[must_use]
    pub fn accept_compressed(mut self, accept: bool) -> Self { self.config = self.config.accept_compressed(accept); self }
    /// Cap every RPC even when the client omits `grpc-timeout`. Distinct from [`Self::timeout`], which sets it. Interceptors and handlers read the same overlay on [`::pbrs_grpc::Rpc::rpc_timeout`] / [`::pbrs_grpc::Request::rpc_timeout`]. See [`::pbrs_grpc::Server::rpc_timeout`].
    #[must_use]
    pub fn rpc_timeout(&self) -> ::core::option::Option<::std::time::Duration> { self.config.rpc_timeout() }
    /// Whether responses are gzipped when the client accepts gzip. Distinct from [`Self::send_compressed`], which enables it. See [`::pbrs_grpc::Server::compresses_outbound`].
    #[must_use]
    pub fn compresses_outbound(&self) -> bool { self.config.compresses_outbound() }
    /// Configured outbound gzip deflate level. Distinct from [`Self::gzip_compression_level`], which sets it. See [`::pbrs_grpc::Server::gzip_level`].
    #[must_use]
    pub fn gzip_level(&self) -> u32 { self.config.gzip_level() }
    /// Whether inbound gzip is inflated. Default `true`. Distinct from [`Self::accept_compressed`], which sets it. Distinct from [`::pbrs_grpc::Rpc::accepts_gzip`], which is the peer's `grpc-accept-encoding`. See [`::pbrs_grpc::Server::accepts_compressed`].
    #[must_use]
    pub fn accepts_compressed(&self) -> bool { self.config.accepts_compressed() }
    /// HTTP/2 PING keepalive. Applies to every call shape. See [`::pbrs_grpc::ServerConfig::keep_alive_interval`].
    #[must_use]
    pub fn keep_alive_interval(mut self, interval: ::std::time::Duration) -> Self { self.config = self.config.keep_alive_interval(interval); self }
    /// How long to wait for a PING acknowledgement. Applies to every call shape. See [`::pbrs_grpc::ServerConfig::keep_alive_timeout`].
    #[must_use]
    pub fn keep_alive_timeout(mut self, timeout: ::std::time::Duration) -> Self { self.config = self.config.keep_alive_timeout(timeout); self }
    /// TCP `SO_KEEPALIVE`. Applies to every call shape. See [`::pbrs_grpc::ServerConfig::tcp_keepalive`].
    #[must_use]
    pub fn tcp_keepalive(mut self, time: ::std::time::Duration) -> Self { self.config = self.config.tcp_keepalive(time); self }
    /// TCP `TCP_KEEPINTVL` probe interval. Applies only when [`Self::tcp_keepalive`] is set; this does not turn `SO_KEEPALIVE` on by itself. Distinct from [`Self::keep_alive_interval`] (HTTP/2 PING). See [`::pbrs_grpc::ServerConfig::tcp_keepalive_interval`].
    #[must_use]
    pub fn tcp_keepalive_interval(mut self, interval: ::std::time::Duration) -> Self { self.config = self.config.tcp_keepalive_interval(interval); self }
    /// TCP `TCP_KEEPCNT` probe count. Applies only when [`Self::tcp_keepalive`] is set; this does not turn `SO_KEEPALIVE` on by itself. Distinct from [`Self::tcp_keepalive_interval`] (`TCP_KEEPINTVL` time, not count). See [`::pbrs_grpc::ServerConfig::tcp_keepalive_retries`].
    #[must_use]
    pub fn tcp_keepalive_retries(mut self, retries: u32) -> Self { self.config = self.config.tcp_keepalive_retries(retries); self }
    /// Send GOAWAY this long after accept. The next RPC of every call shape redials, including over TLS, mTLS, and Unix; transparent retry of the same in-flight RPC is unary and server-streaming after request bytes, client-streaming and bidi before HEADERS. See [`::pbrs_grpc::ServerConfig::max_connection_age`].
    #[must_use]
    pub fn max_connection_age(mut self, age: ::std::time::Duration) -> Self { self.config = self.config.max_connection_age(age); self }
    /// Send GOAWAY after this long with no outstanding RPCs. The next RPC of every call shape redials, including over TLS, mTLS, and Unix. See [`::pbrs_grpc::ServerConfig::max_connection_idle`].
    #[must_use]
    pub fn max_connection_idle(mut self, idle: ::std::time::Duration) -> Self { self.config = self.config.max_connection_idle(idle); self }
    /// After age or idle fires, wait this long for in-flight RPCs, including over TLS, mTLS, Unix, and [`::pbrs_grpc::Server::serve_connection`]. Applies to every call shape. See [`::pbrs_grpc::ServerConfig::max_connection_age_grace`].
    #[must_use]
    pub fn max_connection_age_grace(mut self, grace: ::std::time::Duration) -> Self { self.config = self.config.max_connection_age_grace(grace); self }
    /// Drop a client that never finishes TLS or the HTTP/2 preface. Applies to every call shape, including over TLS, mTLS, and Unix. See [`::pbrs_grpc::ServerConfig::handshake_timeout`].
    #[must_use]
    pub fn handshake_timeout(mut self, timeout: ::std::time::Duration) -> Self { self.config = self.config.handshake_timeout(timeout); self }
    /// Run `interceptor` before `CompatService` methods. It may mutate metadata, cap the deadline (`set_timeout` / `deadline` Instant), inspect path / service / method, `:authority` / `:scheme` / `peer_timeout` / `rpc_timeout` / `effective_timeout` / `local_addr` / `remote_addr` / `peer_identity` / `peer_cred` / message caps / gzip accept and encoding / `compresses_outbound` / `gzip_level` / `accepts_compressed` / `concurrent_rpc_limit` / `send_buffer_size`, attach extensions, or reject. Generated handlers see the same values on [`::pbrs_grpc::Request`]. Calling this twice stacks: the first interceptor runs first. Applies to every call shape; `Err` rejects before the body is read, including over TLS, mTLS, Unix, and [`::pbrs_grpc::Server::serve_connection`]. `Err` may carry [`::pbrs_grpc::Status::with_error_details`]; those trailers reach the client.
    /// [`::pbrs_grpc::Status::from_error_details`] is the typed bag after a generated server intercept Err; those trailers reach the client without reading the body.
    /// Distinct from a generated handler Err: that is after the handler ran; this generated server intercept Err is trailers without reading the body.
    /// Distinct from a generated server on_response Err: that is trailers-only after handler Ok; this generated server intercept Err is trailers without reading the body.
    /// Distinct from a generated intercept Err: that is a local reject never opens a stream; this generated server intercept Err is trailers without reading the body.
    /// Distinct from a generated client on_response Err: that fails the Call after a successful receive; this generated server intercept Err is trailers without reading the body.
    /// Distinct from a generated StreamSender fail: that is trailers after any messages already sent; this generated server intercept Err is trailers without reading the body.
    /// Distinct from [`::pbrs_grpc::Channel::intercept`]: that runs on the outbound call before the stream opens; this generated server intercept runs on the inbound RPC before the handler.
    /// Distinct from [`Self::on_response`]: that runs after the handler returns Ok; this generated server intercept runs on the inbound RPC before the handler.
    /// Compiling overlay dumps live on [`::pbrs_grpc::hello`] (`GreeterServer::new(Svc).intercept`).
    #[must_use]
    pub fn intercept<I>(self, interceptor: I) -> ::pbrs_grpc::Server<Self>
    where
        I: ::pbrs_grpc::Interceptor,
    {
        self.into_server().intercept(interceptor)
    }
    /// Run `interceptor` after `CompatService` methods return `Ok`. It may mutate headers, trailers, compress, and local extensions (`Response::extensions` is not on the wire; stamp metadata here to send it). Calling this twice stacks: the first interceptor runs first. Applies to every call shape, including over TLS, mTLS, Unix, and [`::pbrs_grpc::Server::serve_connection`]. `Err` after the handler already ran; that status is sent trailers-only instead of the response, including [`::pbrs_grpc::Status::with_error_details`]. A handler `Err` skips this hook. [`::pbrs_grpc::ResponseParts::path`] is kernel-stamped. Distinct from [`::pbrs_grpc::Request::path`]. [`::pbrs_grpc::ResponseParts::gzip_level`] is the server encode overlay. Distinct from [`::pbrs_grpc::ResponseParts::compress`]. [`::pbrs_grpc::ResponseParts::compresses_outbound`] is the server encode overlay. Distinct from [`::pbrs_grpc::ResponseParts::compress`], which is the per-RPC Compressed-Flag. [`::pbrs_grpc::ResponseParts::accepts_gzip`] is the peer `grpc-accept-encoding` advertisement. Distinct from [`::pbrs_grpc::ResponseParts::encoding`]. [`::pbrs_grpc::ResponseParts::deadline`] is kernel-stamped when writing. Distinct from [`::pbrs_grpc::Request::deadline`]. Distinct from [`::pbrs_grpc::Rpc::deadline`]. [`::pbrs_grpc::ResponseParts::timeout`] is the duration stamped at dispatch. Distinct from [`::pbrs_grpc::ResponseParts::deadline`]. [`::pbrs_grpc::ResponseParts::limits`] is the encode cap when writing. Distinct from [`::pbrs_grpc::Request::limits`]. Distinct from [`::pbrs_grpc::Rpc::limits`]. [`::pbrs_grpc::ResponseParts::peer_timeout`] is the client's `grpc-timeout`. Distinct from [`::pbrs_grpc::ResponseParts::timeout`]. [`::pbrs_grpc::ResponseParts::rpc_timeout`] is the server overlay. Distinct from [`::pbrs_grpc::ResponseParts::timeout`]. Distinct from [`::pbrs_grpc::ResponseParts::peer_timeout`]. [`::pbrs_grpc::ResponseParts::accepts_compressed`] is the inbound gzip overlay. Distinct from [`::pbrs_grpc::ResponseParts::accepts_gzip`]. [`::pbrs_grpc::ResponseParts::send_buffer_size`] is the write-time HTTP/2 send buffer overlay. Distinct from [`::pbrs_grpc::ResponseParts::limits`].
    /// [`::pbrs_grpc::ResponseParts::compress_is_set`] is occupancy after a generated server on_response, so a later interceptor can fill compress only when unset.
    /// [`::pbrs_grpc::ResponseParts::clear_compress`] restores the server gzip overlay after a generated server on_response.
    /// [`::pbrs_grpc::Status::from_error_details`] is the typed bag after a generated server on_response Err; a local reject is trailers-only after handler Ok.
    /// Distinct from a generated handler Err: that is after the handler ran; this generated server on_response Err is trailers-only after handler Ok.
    /// Distinct from a generated server intercept Err: that is trailers without reading the body; this generated server on_response Err is trailers-only after handler Ok.
    /// Distinct from a generated client on_response Err: that fails the Call after a successful receive; this generated server on_response Err is trailers-only after handler Ok.
    /// Distinct from a generated StreamSender fail: that is trailers after any messages already sent; this generated server on_response Err is trailers-only after handler Ok.
    /// Distinct from [`Self::intercept`]: that runs on the inbound RPC before the handler; this generated server on_response runs after the handler returns Ok.
    /// Compiling overlay dumps live on [`::pbrs_grpc::hello`] (`GreeterServer::new(Svc).on_response`).
    #[must_use]
    pub fn on_response<I>(self, interceptor: I) -> ::pbrs_grpc::Server<Self>
    where
        I: ::pbrs_grpc::ResponseInterceptor,
    {
        self.into_server().on_response(interceptor)
    }
    /// Mount alongside another service. [`Self::max_decoding_message_size`] and [`Self::max_encoding_message_size`] stay in effect on every mounted service, on every call shape of those mounts, including over TLS, mTLS, Unix, and [`::pbrs_grpc::Server::serve_connection`].
    #[must_use]
    pub fn add_service<S: ::pbrs_grpc::Service>(self, service: S) -> ::pbrs_grpc::Router { self.into_server().add_service(service) }
    /// Mount `service` when `Some`. `None` is a no-op. Distinct from [`Self::add_service`], which always mounts. `None` does not replace a service already there. Services that stay mounted still complete every call shape, including over TLS, mTLS, Unix, and [`::pbrs_grpc::Server::serve_connection`].
    #[must_use]
    pub fn add_optional_service<S: ::pbrs_grpc::Service>(self, service: ::core::option::Option<S>) -> ::pbrs_grpc::Router { self.into_server().add_optional_service(service) }
    /// Move this service into a [`::pbrs_grpc::Router`], keeping the configuration.
    #[must_use]
    pub fn into_router(self) -> ::pbrs_grpc::Router { self.into_server().into_router() }
    /// Bind `addr` and serve until the listener fails. Applies to every call shape.
    pub async fn serve(self, addr: ::std::net::SocketAddr) -> ::core::result::Result<(), ::pbrs_grpc::Status> { self.into_server().serve(addr).await }
    /// Serve on an existing listener until it fails. Applies to every call shape.
    pub async fn serve_listener(self, listener: ::pbrs_grpc::codegen_support::TcpListener) -> ::core::result::Result<(), ::pbrs_grpc::Status> { self.into_server().serve_listener(listener).await }
    /// Serve until `shutdown` resolves, then drain. Applies to every call shape. In-flight RPCs finish; new connections are refused. TLS and Unix drain the same way (`serve_tls_with_shutdown`, `serve_unix_with_shutdown`).
    pub async fn serve_with_shutdown(self, listener: ::pbrs_grpc::codegen_support::TcpListener, shutdown: impl ::core::future::Future<Output = ()> + Send) -> ::core::result::Result<(), ::pbrs_grpc::Status> { self.into_server().serve_with_shutdown(listener, shutdown).await }
    /// Bind `addr` and serve until `shutdown` resolves, then drain. Applies to every call shape.
    pub async fn serve_until_shutdown(self, addr: ::std::net::SocketAddr, shutdown: impl ::core::future::Future<Output = ()> + Send) -> ::core::result::Result<(), ::pbrs_grpc::Status> { self.into_server().serve_until_shutdown(addr, shutdown).await }
    /// Bind `path` and serve h2c over a Unix domain socket. Applies to every call shape.
    #[cfg(unix)]
    pub async fn serve_unix(self, path: impl AsRef<::std::path::Path>) -> ::core::result::Result<(), ::pbrs_grpc::Status> { self.into_server().serve_unix(path).await }
    /// Bind `path` after unlinking a crash leftover. A live listener is left alone and this fails with UNAVAILABLE. Applies to every call shape.
    #[cfg(unix)]
    pub async fn serve_unix_unlink(self, path: impl AsRef<::std::path::Path>) -> ::core::result::Result<(), ::pbrs_grpc::Status> { self.into_server().serve_unix_unlink(path).await }
    /// Serve h2c on an existing Unix listener. Applies to every call shape.
    #[cfg(unix)]
    pub async fn serve_unix_listener(self, listener: ::pbrs_grpc::codegen_support::UnixListener) -> ::core::result::Result<(), ::pbrs_grpc::Status> { self.into_server().serve_unix_listener(listener).await }
    /// Serve h2c on a Unix listener until `shutdown` resolves, then drain. Applies to every call shape. In-flight RPCs finish; new connections are refused. See [`::pbrs_grpc::Server::serve_with_shutdown`].
    #[cfg(unix)]
    pub async fn serve_unix_with_shutdown(self, listener: ::pbrs_grpc::codegen_support::UnixListener, shutdown: impl ::core::future::Future<Output = ()> + Send) -> ::core::result::Result<(), ::pbrs_grpc::Status> { self.into_server().serve_unix_with_shutdown(listener, shutdown).await }
    /// Bind `path` and serve h2c until `shutdown` resolves, then drain. Applies to every call shape.
    #[cfg(unix)]
    pub async fn serve_unix_until_shutdown(self, path: impl AsRef<::std::path::Path>, shutdown: impl ::core::future::Future<Output = ()> + Send) -> ::core::result::Result<(), ::pbrs_grpc::Status> { self.into_server().serve_unix_until_shutdown(path, shutdown).await }
    /// Bind `path` until `shutdown` after unlinking a crash leftover. A live listener is left alone. Applies to every call shape.
    #[cfg(unix)]
    pub async fn serve_unix_unlink_until_shutdown(self, path: impl AsRef<::std::path::Path>, shutdown: impl ::core::future::Future<Output = ()> + Send) -> ::core::result::Result<(), ::pbrs_grpc::Status> { self.into_server().serve_unix_unlink_until_shutdown(path, shutdown).await }
    /// Bind `addr` and serve over TLS until the listener fails. Applies to every call shape.
    pub async fn serve_tls(self, addr: ::std::net::SocketAddr, tls: ::pbrs_grpc::ServerTls) -> ::core::result::Result<(), ::pbrs_grpc::Status> { self.into_server().serve_tls(addr, tls).await }
    /// Serve over TLS until `shutdown` resolves, then drain. Applies to every call shape, including mTLS. In-flight RPCs finish; new connections are refused.
    pub async fn serve_tls_with_shutdown(self, listener: ::pbrs_grpc::codegen_support::TcpListener, shutdown: impl ::core::future::Future<Output = ()> + Send, tls: ::pbrs_grpc::ServerTls) -> ::core::result::Result<(), ::pbrs_grpc::Status> { self.into_server().serve_tls_with_shutdown(listener, shutdown, tls).await }
    /// Bind `addr` and serve over TLS until `shutdown` resolves, then drain. Applies to every call shape.
    pub async fn serve_tls_until_shutdown(self, addr: ::std::net::SocketAddr, shutdown: impl ::core::future::Future<Output = ()> + Send, tls: ::pbrs_grpc::ServerTls) -> ::core::result::Result<(), ::pbrs_grpc::Status> { self.into_server().serve_tls_until_shutdown(addr, shutdown, tls).await }
    /// Serve a single already-accepted byte stream until it closes. Applies to every call shape. Generated handlers see empty peer facts on [`::pbrs_grpc::Request`] / [`::pbrs_grpc::Parts`]; [`::pbrs_grpc::Rpc::scheme`] is the peer's `:scheme`. See [`::pbrs_grpc::Server::serve_connection`].
    pub async fn serve_connection<IO>(self, io: IO) -> ::core::result::Result<(), ::pbrs_grpc::Status>
    where
        IO: ::pbrs_grpc::codegen_support::AsyncRead + ::pbrs_grpc::codegen_support::AsyncWrite + Unpin + Send + 'static,
    {
        self.into_server().serve_connection(io).await
    }
    /// Serve connections from `incoming` until it is exhausted. Applies to every call shape. Override [`::pbrs_grpc::Incoming::peer`] to stamp connection facts.
    pub async fn serve_with_incoming<I: ::pbrs_grpc::Incoming>(self, incoming: I) -> ::core::result::Result<(), ::pbrs_grpc::Status> { self.into_server().serve_with_incoming(incoming).await }
    /// Serve from `incoming` until `shutdown` resolves, then drain. Applies to every call shape.
    pub async fn serve_with_incoming_shutdown<I: ::pbrs_grpc::Incoming>(self, incoming: I, shutdown: impl ::core::future::Future<Output = ()> + Send) -> ::core::result::Result<(), ::pbrs_grpc::Status> { self.into_server().serve_with_incoming_shutdown(incoming, shutdown).await }
    fn into_server(self) -> ::pbrs_grpc::Server<Self> { let config = self.config; ::pbrs_grpc::Server::new(self).config(config) }
}
impl<T: CompatService> ::pbrs_grpc::Service for CompatServiceServer<T> {
    const NAME: &'static str = "compat.CompatService";
    fn call(&self, rpc: ::pbrs_grpc::Rpc) -> impl ::core::future::Future<Output = ()> + Send {
        let inner = ::std::sync::Arc::clone(&self.inner);
        async move {
            match rpc.method() {
                "BidiStreamCall" => {
                    rpc.bidi_streaming(move |request| async move { inner.bidi_stream_call(request).await }).await;
                }
                "ClientStreamCall" => {
                    rpc.client_streaming(move |request| async move { inner.client_stream_call(request).await }).await;
                }
                "ServerStreamCall" => {
                    rpc.server_streaming(move |request| async move { inner.server_stream_call(request).await }).await;
                }
                "UnaryCall" => {
                    rpc.unary(move |request| async move { inner.unary_call(request).await }).await;
                }
                _ => rpc.unimplemented(),
            }
        }
    }
}
/// Client for `compat.CompatService`.
///
/// Wraps a [`::pbrs_grpc::Channel`]; cloning is cheap and shares connections.
/// Dial with [`Self::connect`], [`Self::connect_tls`], or [`Self::from_io`].
/// On Unix, `connect_unix` takes a filesystem path. Wrap an existing channel with [`Self::new`].
/// [`Self::authority`], [`Self::scheme`], and [`Self::grpc_user_agent`] read the same values interceptors see on [`::pbrs_grpc::Outgoing`]. [`Self::config`] is the channel overlay those values come from. [`Self::rpc_timeout`], [`Self::waits_for_ready`], [`Self::compresses_outbound`], [`Self::gzip_level`], [`Self::accepts_compressed`], [`Self::concurrent_rpc_limit`], [`Self::stream_buffer_size`], [`Self::send_buffer_size`], and [`Self::limits`] read that overlay without colliding with the setters.
/// [`Self::connected`] is the live-socket snapshot. Distinct from [`Self::waits_for_ready`]. Same snapshot as [`::pbrs_grpc::Channel::connected`].
#[derive(::core::clone::Clone)]
pub struct CompatServiceClient {
    channel: ::pbrs_grpc::Channel,
}
impl ::core::fmt::Debug for CompatServiceClient {
    fn fmt(&self, f: &mut ::core::fmt::Formatter<'_>) -> ::core::fmt::Result {
        f.debug_struct("CompatServiceClient").field("channel", &self.channel).finish()
    }
}
impl CompatServiceClient {
    /// Fully qualified proto service name.
    pub const NAME: &'static str = "compat.CompatService";
    /// Wrap a channel.
    #[must_use]
    pub fn new(channel: ::pbrs_grpc::Channel) -> Self { Self { channel } }
    /// Take the channel back.
    #[must_use]
    pub fn into_inner(self) -> ::pbrs_grpc::Channel { self.channel }
    /// Dial `target` (`host:port`, not a tonic `http://` / `https://` URI). See [`::pbrs_grpc::Channel::connect`]. Applies to every call shape.
    pub async fn connect(target: impl Into<::pbrs_grpc::Target>) -> ::core::result::Result<Self, ::pbrs_grpc::Status> {
        Ok(Self::new(::pbrs_grpc::Channel::connect(target).await?))
    }
    /// [`Self::connect`] with [`::pbrs_grpc::ChannelConfig`]. Applies to every call shape.
    pub async fn connect_with(target: impl Into<::pbrs_grpc::Target>, config: ::pbrs_grpc::ChannelConfig) -> ::core::result::Result<Self, ::pbrs_grpc::Status> {
        Ok(Self::new(::pbrs_grpc::Channel::connect_with(target, config).await?))
    }
    /// Open `connections` slots. See [`::pbrs_grpc::Channel::connect_pool`]. Applies to every call shape.
    /// TLS (including mTLS) pooling is [`Self::connect_tls_with`] plus [`::pbrs_grpc::ChannelConfig::connections`]; Unix is [`Self::connect_unix_with`]. [`::pbrs_grpc::Channel::from_io`] cannot pool.
    /// A pool larger than the server's [`::pbrs_grpc::Server::max_concurrent_connections`] fails the dial as `UNAVAILABLE`.
    pub async fn connect_pool(target: impl Into<::pbrs_grpc::Target>, connections: usize) -> ::core::result::Result<Self, ::pbrs_grpc::Status> {
        Ok(Self::new(::pbrs_grpc::Channel::connect_pool(target, connections).await?))
    }
    /// Dial over TLS. See [`::pbrs_grpc::Channel::connect_tls`]. Applies to every call shape.
    pub async fn connect_tls(target: impl Into<::pbrs_grpc::Target>, tls: ::pbrs_grpc::ClientTls) -> ::core::result::Result<Self, ::pbrs_grpc::Status> {
        Ok(Self::new(::pbrs_grpc::Channel::connect_tls(target, tls).await?))
    }
    /// [`Self::connect_tls`] with [`::pbrs_grpc::ChannelConfig`]. Applies to every call shape.
    /// [`::pbrs_grpc::ChannelConfig::connections`] opens that many TLS sockets (including mTLS); all must succeed. [`::pbrs_grpc::Channel::from_io`] cannot pool.
    pub async fn connect_tls_with(target: impl Into<::pbrs_grpc::Target>, config: ::pbrs_grpc::ChannelConfig, tls: ::pbrs_grpc::ClientTls) -> ::core::result::Result<Self, ::pbrs_grpc::Status> {
        Ok(Self::new(::pbrs_grpc::Channel::connect_tls_with(target, config, tls).await?))
    }
    /// Dial on the first RPC. See [`::pbrs_grpc::Channel::connect_lazy`]. Applies to every call shape.
    pub fn connect_lazy(target: impl Into<::pbrs_grpc::Target>) -> ::core::result::Result<Self, ::pbrs_grpc::Status> {
        Ok(Self::new(::pbrs_grpc::Channel::connect_lazy(target)?))
    }
    /// [`Self::connect_lazy`] with [`::pbrs_grpc::ChannelConfig`]. Applies to every call shape.
    pub fn connect_lazy_with(target: impl Into<::pbrs_grpc::Target>, config: ::pbrs_grpc::ChannelConfig) -> ::core::result::Result<Self, ::pbrs_grpc::Status> {
        Ok(Self::new(::pbrs_grpc::Channel::connect_lazy_with(target, config)?))
    }
    /// [`Self::connect_lazy`] over TLS. See [`::pbrs_grpc::Channel::connect_tls_lazy`]. Applies to every call shape.
    pub fn connect_tls_lazy(target: impl Into<::pbrs_grpc::Target>, tls: ::pbrs_grpc::ClientTls) -> ::core::result::Result<Self, ::pbrs_grpc::Status> {
        Ok(Self::new(::pbrs_grpc::Channel::connect_tls_lazy(target, tls)?))
    }
    /// [`Self::connect_tls_lazy`] with [`::pbrs_grpc::ChannelConfig`]. Applies to every call shape.
    pub fn connect_tls_lazy_with(target: impl Into<::pbrs_grpc::Target>, config: ::pbrs_grpc::ChannelConfig, tls: ::pbrs_grpc::ClientTls) -> ::core::result::Result<Self, ::pbrs_grpc::Status> {
        Ok(Self::new(::pbrs_grpc::Channel::connect_tls_lazy_with(target, config, tls)?))
    }
    /// Dial a Unix domain socket. See [`::pbrs_grpc::Channel::connect_unix`]. Applies to every call shape.
    #[cfg(unix)]
    pub async fn connect_unix(path: impl AsRef<::std::path::Path>) -> ::core::result::Result<Self, ::pbrs_grpc::Status> {
        Ok(Self::new(::pbrs_grpc::Channel::connect_unix(path).await?))
    }
    /// [`Self::connect_unix`] with [`::pbrs_grpc::ChannelConfig`]. Applies to every call shape.
    /// [`::pbrs_grpc::ChannelConfig::connections`] opens that many Unix sockets; all must succeed. [`::pbrs_grpc::Channel::from_io`] cannot pool.
    #[cfg(unix)]
    pub async fn connect_unix_with(path: impl AsRef<::std::path::Path>, config: ::pbrs_grpc::ChannelConfig) -> ::core::result::Result<Self, ::pbrs_grpc::Status> {
        Ok(Self::new(::pbrs_grpc::Channel::connect_unix_with(path, config).await?))
    }
    /// [`Self::connect_unix`] that dials on the first RPC. Applies to every call shape.
    #[cfg(unix)]
    pub fn connect_unix_lazy(path: impl AsRef<::std::path::Path>) -> ::core::result::Result<Self, ::pbrs_grpc::Status> {
        Ok(Self::new(::pbrs_grpc::Channel::connect_unix_lazy(path)?))
    }
    /// [`Self::connect_unix_lazy`] with [`::pbrs_grpc::ChannelConfig`]. Applies to every call shape.
    #[cfg(unix)]
    pub fn connect_unix_lazy_with(path: impl AsRef<::std::path::Path>, config: ::pbrs_grpc::ChannelConfig) -> ::core::result::Result<Self, ::pbrs_grpc::Status> {
        Ok(Self::new(::pbrs_grpc::Channel::connect_unix_lazy_with(path, config)?))
    }
    /// Speak gRPC over an already-connected byte stream. See [`::pbrs_grpc::Channel::from_io`]. Applies to every call shape.
    pub async fn from_io<IO>(io: IO, authority: impl Into<::pbrs_grpc::Target>) -> ::core::result::Result<Self, ::pbrs_grpc::Status>
    where
        IO: ::pbrs_grpc::codegen_support::AsyncRead + ::pbrs_grpc::codegen_support::AsyncWrite + Unpin + Send + 'static,
    {
        Ok(Self::new(::pbrs_grpc::Channel::from_io(io, authority).await?))
    }
    /// [`Self::from_io`] with [`::pbrs_grpc::ChannelConfig`]. Applies to every call shape.
    /// [`::pbrs_grpc::ChannelConfig::connections`] is forced to 1: one duplex is one HTTP/2 connection.
    pub async fn from_io_with<IO>(io: IO, authority: impl Into<::pbrs_grpc::Target>, config: ::pbrs_grpc::ChannelConfig) -> ::core::result::Result<Self, ::pbrs_grpc::Status>
    where
        IO: ::pbrs_grpc::codegen_support::AsyncRead + ::pbrs_grpc::codegen_support::AsyncWrite + Unpin + Send + 'static,
    {
        Ok(Self::new(::pbrs_grpc::Channel::from_io_with(io, authority, config).await?))
    }
    /// Send `:scheme https` from a [`Self::from_io`] channel. See [`::pbrs_grpc::Channel::https_scheme`]. Applies to every call shape.
    #[must_use]
    pub fn https_scheme(self) -> Self { Self { channel: self.channel.https_scheme() } }
    /// Override `:authority` on this clone without changing the dial. See [`::pbrs_grpc::Channel::origin`]. Applies to every call shape.
    pub fn origin(self, authority: impl Into<::pbrs_grpc::Target>) -> ::core::result::Result<Self, ::pbrs_grpc::Status> {
        Ok(Self { channel: self.channel.origin(authority)? })
    }
    /// HTTP/2 `:scheme` this client sends. See [`::pbrs_grpc::Channel::scheme`]. Applies to every call shape.
    #[must_use]
    pub fn scheme(&self) -> &'static str { self.channel.scheme() }
    /// The HTTP/2 `:authority` this client sends. See [`::pbrs_grpc::Channel::authority`]. Applies to every call shape.
    #[must_use]
    pub fn authority(&self) -> &str { self.channel.authority() }
    /// The `user-agent` this client sends. See [`::pbrs_grpc::Channel::grpc_user_agent`]. Applies to every call shape.
    #[must_use]
    pub fn grpc_user_agent(&self) -> &str { self.channel.grpc_user_agent() }
    /// The configuration in effect. See [`::pbrs_grpc::Channel::config`].
    #[must_use]
    pub fn config(&self) -> ::pbrs_grpc::ChannelConfig { self.channel.config() }
    /// Whether any pool slot currently holds a live HTTP/2 connection. Distinct from gRPC `GetState`. See [`::pbrs_grpc::Channel::connected`]. Applies to every call shape. Client interceptors see the same snapshot as [`::pbrs_grpc::Outgoing::connected`]. Distinct from [`Self::wait_for_ready`]: that overlay queues; this getter is a live snapshot.
    #[must_use]
    pub fn connected(&self) -> bool { self.channel.connected() }
    /// Default per-RPC deadline when the request omits one. Distinct from [`Self::timeout`], which sets it. See [`::pbrs_grpc::Channel::rpc_timeout`]. Applies to every call shape.
    #[must_use]
    pub fn rpc_timeout(&self) -> ::core::option::Option<::std::time::Duration> { self.channel.rpc_timeout() }
    /// Whether this client waits for a connection instead of failing fast. See [`::pbrs_grpc::Channel::waits_for_ready`]. Applies to every call shape. Distinct from [`Self::connected`]: that getter is a live snapshot, not this overlay.
    #[must_use]
    pub fn waits_for_ready(&self) -> bool { self.channel.waits_for_ready() }
    /// Whether this client gzips outbound payloads. See [`::pbrs_grpc::Channel::compresses_outbound`]. Applies to every call shape.
    #[must_use]
    pub fn compresses_outbound(&self) -> bool { self.channel.compresses_outbound() }
    /// Configured outbound gzip deflate level. Distinct from [`Self::gzip_compression_level`], which sets it. See [`::pbrs_grpc::Channel::gzip_level`]. Applies to every call shape.
    #[must_use]
    pub fn gzip_level(&self) -> u32 { self.channel.gzip_level() }
    /// Whether inbound gzip is inflated. Default `true`. Distinct from [`Self::accept_compressed`], which sets it. Distinct from [`::pbrs_grpc::Rpc::accepts_gzip`], which is the peer's `grpc-accept-encoding`. See [`::pbrs_grpc::Channel::accepts_compressed`]. Applies to every call shape.
    #[must_use]
    pub fn accepts_compressed(&self) -> bool { self.channel.accepts_compressed() }
    /// Configured channel-wide RPC cap, if any. Distinct from [`Self::max_concurrent_rpcs`], which sets it. See [`::pbrs_grpc::Channel::concurrent_rpc_limit`]. Applies to every call shape.
    #[must_use]
    pub fn concurrent_rpc_limit(&self) -> ::core::option::Option<usize> { self.channel.concurrent_rpc_limit() }
    /// Configured outbound streaming queue depth. Distinct from [`Self::stream_buffer`], which sets it. Distinct from [`Self::message_limits`]: that is message size, not queue depth. See [`::pbrs_grpc::Channel::stream_buffer_size`]. Applies to client-streaming and bidi request streams.
    #[must_use]
    pub fn stream_buffer_size(&self) -> usize { self.channel.stream_buffer_size() }
    /// Configured write-time HTTP/2 send buffer. Distinct from [`Self::max_send_buffer_size`], which sets it. Distinct from [`Self::stream_buffer_size`]: that is queue depth, not this send buffer. See [`::pbrs_grpc::Channel::send_buffer_size`]. Applies to every call shape.
    #[must_use]
    pub fn send_buffer_size(&self) -> usize { self.channel.send_buffer_size() }
    /// Configured message caps. Distinct from [`Self::message_limits`], which sets them. Distinct from [`Self::stream_buffer_size`]: that is queue depth, not uncompressed protobuf bytes. Distinct from [`Self::send_buffer_size`]: that is the HTTP/2 send buffer, not these caps. See [`::pbrs_grpc::Channel::limits`]. Applies to every call shape.
    #[must_use]
    pub fn limits(&self) -> ::pbrs_grpc::MessageLimits { self.channel.limits() }
    /// Cap inbound messages at `limit` bytes. Default 4 MiB. Applies to every call shape, including over TLS, mTLS, Unix, and [`::pbrs_grpc::Channel::from_io`].
    #[must_use]
    pub fn max_decoding_message_size(mut self, limit: usize) -> Self { self.channel = self.channel.max_decoding_message_size(limit); self }
    /// Cap outbound messages at `limit` bytes. Default unlimited. Applies to every call shape, including over TLS, mTLS, Unix, and [`::pbrs_grpc::Channel::from_io`].
    #[must_use]
    pub fn max_encoding_message_size(mut self, limit: usize) -> Self { self.channel = self.channel.max_encoding_message_size(limit); self }
    /// Replace both message caps at once. Applies to every call shape. See [`::pbrs_grpc::Channel::message_limits`].
    /// Distinct from [`Self::max_encoding_message_size`] / [`Self::max_decoding_message_size`]. Oversize is `RESOURCE_EXHAUSTED` on every call shape, including over TLS, mTLS, Unix, and [`::pbrs_grpc::Channel::from_io`].
    #[must_use]
    pub fn message_limits(mut self, limits: ::pbrs_grpc::MessageLimits) -> Self { self.channel = self.channel.message_limits(limits); self }
    /// Run `interceptor` on every outbound RPC before the stream opens. Invoked when the generated method is called, not on first poll of the [`::pbrs_grpc::Call`]. `Err` fails that Call on poll, including [`::pbrs_grpc::Status::with_error_details`]; nothing is sent. A local [`::pbrs_grpc::Status::with_error_details`] is [`::pbrs_grpc::Status::rpc`] / [`::pbrs_grpc::Status::error_details`] on that Call for every call shape. [`::pbrs_grpc::Outgoing::set_timeout`] is that Call's deadline on every call shape. `clear_timeout` opts out of the channel timeout on every call shape. `clear_compress` then `set_compress(compresses_outbound())` reapplies channel gzip on every call shape. `set_compress` stamps [`::pbrs_grpc::StreamSender::compress`] on client-streaming and bidi request streams. Outgoing getters apply to every call shape. `clear_user_agent` restores the channel user-agent after a generated intercept prefix.
    /// The interceptor sees a [`::pbrs_grpc::Outgoing`]: path, service, method, `:authority`, `:scheme`,
    /// `user-agent` (`user_agent_is_set`), message caps, metadata, timeout / deadline Instant, wait-for-ready (`wait_for_ready_is_set`), compression (`compress_is_set`), channel overlays (`rpc_timeout` / `waits_for_ready` / `compresses_outbound` / `gzip_level` / `accepts_compressed` / `concurrent_rpc_limit` / `stream_buffer_size` / `send_buffer_size` / `limits`; `clear_*` opts out of the already-applied default), extensions, `connected` (same snapshot as [`::pbrs_grpc::Channel::connected`]). [`::pbrs_grpc::Outgoing::set_user_agent`] prefixes this RPC. [`::pbrs_grpc::Request::set_user_agent`] is the same prefix at the call site. [`::pbrs_grpc::Outgoing::connected`] is the live-socket snapshot. Distinct from wait-for-ready: a lazy first RPC sees `false` even when that overlay is on. [`::pbrs_grpc::Outgoing::gzip_level`] is deflate effort. Distinct from [`::pbrs_grpc::Outgoing::compresses_outbound`] (on or off). An interceptor cannot change it. [`::pbrs_grpc::Outgoing::accepts_compressed`] is the inbound gzip overlay (default on). [`::pbrs_grpc::Outgoing::limits`] is the channel message-cap overlay. Same overlay as [`::pbrs_grpc::Channel::limits`]. [`::pbrs_grpc::Outgoing::concurrent_rpc_limit`] is the channel RPC cap overlay. Distinct from [`::pbrs_grpc::Outgoing::waits_for_ready`]: that waits for a connection; this refuses extras. [`::pbrs_grpc::Outgoing::stream_buffer_size`] is the outbound streaming queue overlay. Distinct from [`::pbrs_grpc::Outgoing::limits`]: that is message size, not queue depth. [`::pbrs_grpc::Outgoing::send_buffer_size`] is the outbound HTTP/2 send buffer overlay. Distinct from [`::pbrs_grpc::Outgoing::stream_buffer_size`]: that is queue depth, not this send buffer.
    /// Compiling overlay dumps live on [`::pbrs_grpc::hello`] (`GreeterClient::new(channel).intercept`).
    /// [`::pbrs_grpc::Outgoing::user_agent_is_set`] is occupancy on this generated intercept path, so a later interceptor can prefix only when unset.
    /// [`::pbrs_grpc::Outgoing::wait_for_ready_is_set`] is occupancy on this generated intercept path, so a later interceptor can fill wait-for-ready only when unset.
    /// [`::pbrs_grpc::Outgoing::compress_is_set`] is occupancy on this generated intercept path, so a later interceptor can fill compress only when unset.
    /// `clear_wait_for_ready` restores the channel wait-for-ready overlay after a generated intercept choice.
    /// `clear_timeout` opts out of the channel timeout after a generated intercept choice.
    /// `clear_compress` then `set_compress` from `compresses_outbound` reapplies channel gzip after a generated intercept choice.
    /// [`::pbrs_grpc::Outgoing::connected`] is the live-socket snapshot on this generated intercept path ([`::pbrs_grpc::Channel::connected`]), taken when the interceptor runs. Distinct from wait-for-ready: a lazy first RPC sees `false` even when that overlay is on.
    /// [`::pbrs_grpc::Status::from_error_details`] is the typed bag after a generated intercept Err; a local reject never opens a stream.
    /// Distinct from a generated handler Err: that is after the handler ran; this generated intercept Err is a local reject never opens a stream.
    /// Distinct from a generated client on_response Err: that fails the Call after a successful receive; this generated intercept Err is a local reject never opens a stream.
    /// Distinct from a generated server intercept Err: that is trailers without reading the body; this generated intercept Err is a local reject never opens a stream.
    /// Distinct from a generated StreamSender fail: that is trailers after any messages already sent; this generated intercept Err is a local reject never opens a stream.
    /// Distinct from [`::pbrs_grpc::Channel::max_concurrent_rpcs`]: that takes a slot when the [`::pbrs_grpc::Call`] is polled; a generated intercept already ran, so a local Err never consumes that budget.
    /// Distinct from [`::pbrs_grpc::Server::intercept`]: that runs on the inbound RPC before the handler; this generated intercept runs on the outbound call before the stream opens.
    /// Distinct from [`Self::on_response`]: that runs after a successful receive; this generated intercept runs on the outbound call before the stream opens.
    #[must_use]
    pub fn intercept<I>(self, interceptor: I) -> Self
    where
        I: ::pbrs_grpc::ClientInterceptor,
    {
        Self { channel: self.channel.intercept(interceptor) }
    }
    /// Run `interceptor` after a successful receive, before the [`::pbrs_grpc::Call`] is Ready. Closures implement [`::pbrs_grpc::ResponseInterceptor`]. A received reply starts empty; this is how a client inserts typed context after the peer cannot. Distinct from [`Self::intercept`], which runs before the stream opens. Calling this twice stacks: the first interceptor runs first. Applies to every call shape, including over TLS, mTLS, Unix, and [`::pbrs_grpc::Channel::from_io`]. `Err` fails that Call (the peer already sent OK), including [`::pbrs_grpc::Status::with_error_details`]. A non-OK peer status skips this hook. On server-streaming and bidi, trailers on this envelope do not replace [`::pbrs_grpc::Streaming::trailers`]. See [`::pbrs_grpc::Channel::on_response`]. [`::pbrs_grpc::ResponseParts::path`] is kernel-stamped. Distinct from [`::pbrs_grpc::Outgoing::path`]. [`::pbrs_grpc::Response::gzip_level`] on a received reply is not the peer's deflate effort. Distinct from [`::pbrs_grpc::Response::encoding`]. [`::pbrs_grpc::Response::compresses_outbound`] on a received reply is `false` (the overlay is not on the wire). [`::pbrs_grpc::Response::accepts_gzip`] on a received reply is `false` (the advertisement is not on the reply wire). [`::pbrs_grpc::Response::deadline`] on a received reply is `None` (the peer deadline is not on the wire). [`::pbrs_grpc::Response::timeout`] on a received reply is `None` (the peer timeout is not on the reply wire). [`::pbrs_grpc::Response::limits`] on a received reply is `None` (the peer encode cap is not on the wire). [`::pbrs_grpc::Response::peer_timeout`] on a received reply is `None` (the client's `grpc-timeout` is not on the reply wire). [`::pbrs_grpc::Response::rpc_timeout`] on a received reply is `None` (the server overlay is not on the reply wire). [`::pbrs_grpc::Response::accepts_compressed`] on a received reply is `false` (this overlay is not a received-reply field). [`::pbrs_grpc::Response::send_buffer_size`] on a received reply is `None` (the peer send buffer is not on the reply wire).
    /// [`::pbrs_grpc::ResponseParts::compress_is_set`] is occupancy after a generated client on_response, so a later interceptor can fill compress only when unset.
    /// [`::pbrs_grpc::ResponseParts::clear_compress`] drops a compress choice after a generated client on_response; a received reply has no server gzip overlay to restore.
    /// [`::pbrs_grpc::Status::from_error_details`] is the typed bag after a generated client on_response Err; a local reject fails the Call after a successful receive.
    /// Distinct from a generated handler Err: that is after the handler ran; this generated client on_response Err fails the Call after a successful receive.
    /// Distinct from a generated server intercept Err: that is trailers without reading the body; this generated client on_response Err fails the Call after a successful receive.
    /// Distinct from a generated intercept Err: that is a local reject never opens a stream; this generated client on_response Err fails the Call after a successful receive.
    /// Distinct from a generated server on_response Err: that is trailers-only after handler Ok; this generated client on_response Err fails the Call after a successful receive.
    /// Distinct from a generated StreamSender fail: that is trailers after any messages already sent; this generated client on_response Err fails the Call after a successful receive.
    /// Distinct from [`Self::intercept`]: that runs on the outbound call before the stream opens; this generated client on_response runs after a successful receive.
    /// Compiling overlay dumps live on [`::pbrs_grpc::hello`] (`GreeterClient::new(channel).on_response`).
    #[must_use]
    pub fn on_response<I>(self, interceptor: I) -> Self
    where
        I: ::pbrs_grpc::ResponseInterceptor,
    {
        Self { channel: self.channel.on_response(interceptor) }
    }
    /// Prefix the kernel `user-agent`. See [`::pbrs_grpc::Channel::user_agent`]. Applies to every call shape.
    pub fn user_agent(self, prefix: impl AsRef<str>) -> ::core::result::Result<Self, ::pbrs_grpc::Status> {
        Ok(Self { channel: self.channel.user_agent(prefix)? })
    }
    /// gzip unary and server-streaming request payloads and [`::pbrs_grpc::StreamSender::send`]. Applies to every call shape, including over TLS, mTLS, Unix, and [`::pbrs_grpc::Channel::from_io`]. See [`::pbrs_grpc::Channel::send_compressed`].
    #[must_use]
    pub fn send_compressed(mut self) -> Self { self.channel = self.channel.send_compressed(); self }
    /// Deflate effort for outbound gzip. Default 1 (`flate2` fast). Applies to every call shape, including over TLS, mTLS, Unix, and [`::pbrs_grpc::Channel::from_io`]. Distinct from [`Self::send_compressed`], which is on or off. 0 stores; 9 is best. See [`::pbrs_grpc::Channel::gzip_compression_level`].
    #[must_use]
    pub fn gzip_compression_level(mut self, level: u32) -> Self { self.channel = self.channel.gzip_compression_level(level); self }
    /// Inflate inbound gzip. Default `true`. Applies to every call shape, including over TLS, mTLS, Unix, and [`::pbrs_grpc::Channel::from_io`]. Passing `false` omits gzip from `grpc-accept-encoding` and refuses a gzip reply as `UNIMPLEMENTED`. Distinct from [`Self::send_compressed`], which is outbound. See [`::pbrs_grpc::Channel::accept_compressed`].
    #[must_use]
    pub fn accept_compressed(mut self, accept: bool) -> Self { self.channel = self.channel.accept_compressed(accept); self }
    /// Default per-RPC deadline when the request omits one. See [`::pbrs_grpc::Channel::timeout`]. Applies to every call shape, including over TLS, mTLS, Unix, and [`::pbrs_grpc::Channel::from_io`]. Interceptors run after this fill and can still set or [`::pbrs_grpc::Outgoing::clear_timeout`].
    #[must_use]
    pub fn timeout(mut self, timeout: ::std::time::Duration) -> Self { self.channel = self.channel.timeout(timeout); self }
    /// Wait for a connection instead of failing fast. See [`::pbrs_grpc::Channel::wait_for_ready`]. Applies to every call shape.
    /// A request that already called [`::pbrs_grpc::Request::set_wait_for_ready`] is left alone. Interceptors run after this fill and can still set or clear it.
    #[must_use]
    pub fn wait_for_ready(mut self) -> Self { self.channel = self.channel.wait_for_ready(); self }
    /// How many messages sit between a client-streaming caller and the wire. See [`::pbrs_grpc::Channel::stream_buffer`]. Applies to client-streaming and bidi request streams.
    #[must_use]
    pub fn stream_buffer(mut self, messages: usize) -> Self { self.channel = self.channel.stream_buffer(messages); self }
    /// Write-time HTTP/2 send buffer threshold for outbound DATA. See [`::pbrs_grpc::Channel::max_send_buffer_size`]. Applies to every call shape, including over TLS, mTLS, Unix, and [`::pbrs_grpc::Channel::from_io`]. Overlay: does not change how a dead slot is redialed.
    #[must_use]
    pub fn max_send_buffer_size(mut self, bytes: usize) -> Self { self.channel = self.channel.max_send_buffer_size(bytes); self }
    /// Cap how many RPCs this channel will run at once. Applies to every call shape, including over TLS, mTLS, Unix, and [`::pbrs_grpc::Channel::from_io`]. Extra RPCs are `RESOURCE_EXHAUSTED` before the stream opens. Distinct from HTTP/2 `SETTINGS_MAX_CONCURRENT_STREAMS`, which waits. Distinct from [`::pbrs_grpc::Server::max_concurrent_rpcs`], which refuses inbound. Clones share the budget. See [`::pbrs_grpc::Channel::max_concurrent_rpcs`].
    #[must_use]
    pub fn max_concurrent_rpcs(mut self, n: usize) -> Self { self.channel = self.channel.max_concurrent_rpcs(n); self }
    /// The channel this client sends on.
    #[must_use]
    pub fn channel(&self) -> &::pbrs_grpc::Channel { &self.channel }
    /// Bidirectional-streaming `BidiStreamCall`.
    /// Interceptors run when this method is called, not on first poll of the [`::pbrs_grpc::Call`].
    /// The reply's [`::pbrs_grpc::Response::encoding`] is the peer's `grpc-encoding` (`None` for identity). The [`::pbrs_grpc::Call`] is fused after it resolves.
    /// Dropping the received [`::pbrs_grpc::Streaming`] before the end resets the RPC.
    /// After end-of-stream or error that stream is fused (`::pbrs_grpc::FusedStream`).
    /// A [`::pbrs_grpc::CallHandle`] taken before await still cancels while waiting for headers, and still cancels that live stream after headers.
    /// Letting the deadline fire RSTs the send half before headers and after headers the same way.
    /// [`::pbrs_grpc::Streaming::trailers`] waits for end-of-stream, including when called before draining messages. A non-OK trailing `grpc-status` is `Err`. A `-bin` trailer must not appear as a header, including over TLS, mTLS, Unix, and [`::pbrs_grpc::Channel::from_io`].
    /// [`::pbrs_grpc::StreamSender::fail`] before headers resolves the [`::pbrs_grpc::Call`] with that status, not `UNAVAILABLE` from the reset; after headers the received [`::pbrs_grpc::Streaming`] sees [`::pbrs_grpc::Code::Cancelled`], not that status.
    /// Dropping the pair without awaiting the [`::pbrs_grpc::Call`] resets the stream.
    #[must_use = "dropping a streaming Call resets the stream"]
    /// Bidirectional-streaming call: `BidiStreamCall`.
    /// Streaming signature: Bidirectional-streaming stream of `CompatRequest` -> stream of `CompatResponse`.
    pub fn bidi_stream_call(&self, request: ::pbrs_grpc::Request<()>) -> (::pbrs_grpc::StreamSender<CompatRequest>, ::pbrs_grpc::Call<::pbrs_grpc::Response<::pbrs_grpc::Streaming<CompatResponse>>>) {
        self.channel.bidi("/compat.CompatService/BidiStreamCall", request)
    }
    /// Client-streaming `ClientStreamCall`.
    /// Interceptors run when this method is called, not on first poll of the [`::pbrs_grpc::Call`].
    /// The reply's [`::pbrs_grpc::Response::encoding`] is the peer's `grpc-encoding` (`None` for identity). The [`::pbrs_grpc::Call`] is fused after it resolves.
    /// A [`::pbrs_grpc::CallHandle`] taken before await still cancels after the sender is closed, while the unary response is pending.
    /// Cancelling before any request message (`cancel_after_begin`) is [`::pbrs_grpc::Code::Cancelled`], not OK from a half-close: hold the [`::pbrs_grpc::StreamSender`] until the [`::pbrs_grpc::Call`] settles, including over TLS, mTLS, Unix, and [`::pbrs_grpc::Channel::from_io`].
    /// Dropping the [`::pbrs_grpc::Call`] or letting its deadline fire after that half-close resets the same way.
    /// OK-path custom trailers land on [`::pbrs_grpc::Response::trailers`]; a `-bin` trailer must not appear as a header, including over TLS, mTLS, Unix, and [`::pbrs_grpc::Channel::from_io`].
    /// [`::pbrs_grpc::StreamSender::fail`] resolves the [`::pbrs_grpc::Call`] with that status (no request-side `grpc-status`; the stream is reset with CANCEL).
    /// Dropping the pair without awaiting the [`::pbrs_grpc::Call`] resets the stream.
    #[must_use = "dropping a streaming Call resets the stream"]
    /// Client-streaming call: `ClientStreamCall`.
    /// Streaming signature: Client-streaming stream of `CompatRequest` -> `CompatResponse`.
    pub fn client_stream_call(&self, request: ::pbrs_grpc::Request<()>) -> (::pbrs_grpc::StreamSender<CompatRequest>, ::pbrs_grpc::Call<::pbrs_grpc::Response<CompatResponse>>) {
        self.channel.client_streaming("/compat.CompatService/ClientStreamCall", request)
    }
    /// Server-streaming `ServerStreamCall`.
    /// Interceptors run when this method is called, not on first poll of the [`::pbrs_grpc::Call`].
    /// The reply's [`::pbrs_grpc::Response::encoding`] is the peer's `grpc-encoding` (`None` for identity). The [`::pbrs_grpc::Call`] is fused after it resolves.
    /// Dropping the received [`::pbrs_grpc::Streaming`] before the end resets the RPC.
    /// After end-of-stream or error that stream is fused (`::pbrs_grpc::FusedStream`).
    /// A [`::pbrs_grpc::CallHandle`] taken before await still cancels while waiting for headers, and still cancels that live stream after headers.
    /// Letting the deadline fire RSTs the send half before headers and after headers the same way.
    /// [`::pbrs_grpc::Streaming::trailers`] waits for end-of-stream, including when called before draining messages. A non-OK trailing `grpc-status` is `Err`. A `-bin` trailer must not appear as a header, including over TLS, mTLS, Unix, and [`::pbrs_grpc::Channel::from_io`].
    /// Server-streaming call: `ServerStreamCall`.
    /// Streaming signature: Server-streaming `CompatRequest` -> stream of `CompatResponse`.
    pub fn server_stream_call(&self, request: ::pbrs_grpc::Request<CompatRequest>) -> ::pbrs_grpc::Call<::pbrs_grpc::Response<::pbrs_grpc::Streaming<CompatResponse>>> {
        self.channel.server_streaming("/compat.CompatService/ServerStreamCall", request)
    }
    /// Unary `UnaryCall`.
    /// Interceptors run when this method is called, not on first poll of the [`::pbrs_grpc::Call`].
    /// The reply's [`::pbrs_grpc::Response::encoding`] is the peer's `grpc-encoding` (`None` for identity). The [`::pbrs_grpc::Call`] is fused after it resolves.
    /// A [`::pbrs_grpc::CallHandle`] taken before await cancels the RPC.
    /// OK-path custom trailers land on [`::pbrs_grpc::Response::trailers`]; a `-bin` trailer must not appear as a header, including over TLS, mTLS, Unix, and [`::pbrs_grpc::Channel::from_io`].
    /// Unary call: `UnaryCall`.
    /// Streaming signature: Unary `CompatRequest` -> `CompatResponse`.
    pub fn unary_call(&self, request: ::pbrs_grpc::Request<CompatRequest>) -> ::pbrs_grpc::Call<::pbrs_grpc::Response<CompatResponse>> {
        self.channel.unary("/compat.CompatService/UnaryCall", request)
    }
}
impl ::core::convert::From<::pbrs_grpc::Channel> for CompatServiceClient {
    fn from(channel: ::pbrs_grpc::Channel) -> Self { Self::new(channel) }
}
// `CompatService` is the server-side counterpart.
}
#[allow(unused_imports, reason = "generated re-exports may not all be used")]
pub use __gen_compat_proto::*;
